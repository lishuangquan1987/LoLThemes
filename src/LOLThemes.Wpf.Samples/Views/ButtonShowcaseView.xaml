<UserControl
    x:Class="LOLThemes.Wpf.Samples.Views.ButtonShowcaseView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:LOLThemes.Wpf.Controls;assembly=LOLThemes.Wpf"
    xmlns:smtx="clr-namespace:ShowMeTheXAML;assembly=ShowMeTheXAML"
    Background="{DynamicResource BackgroundDarkBrush}">

    <ScrollViewer
        Padding="30"
        HorizontalScrollBarVisibility="Disabled"
        VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <!--  标题  -->
            <Border
                Margin="0,0,0,20"
                Padding="20,15"
                Background="{DynamicResource BackgroundMediumBrush}"
                BorderBrush="{DynamicResource BorderGoldBrush}"
                BorderThickness="0,0,0,2">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,8"
                        FontSize="{DynamicResource FontSizeLarge}"
                        FontWeight="{StaticResource FontWeightBold}"
                        Foreground="{DynamicResource PrimaryGoldBrush}"
                        Text="Button - 按钮" />
                    <TextBlock
                        FontSize="{StaticResource FontSizeNormal}"
                        Foreground="{DynamicResource TextSecondaryBrush}"
                        Text="按钮是用户界面中最常用的控件之一，用于触发操作。" />
                </StackPanel>
            </Border>

            <!--  基础用法  -->
            <Border
                Margin="0,0,0,20"
                Padding="20"
                Background="{DynamicResource BackgroundMediumBrush}"
                BorderBrush="{DynamicResource BorderDarkBrush}"
                BorderThickness="0.5">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,15"
                        FontSize="{StaticResource FontSizeLarge}"
                        FontWeight="{StaticResource FontWeightBold}"
                        Foreground="{DynamicResource PrimaryGoldBrush}"
                        Text="基础用法" />

                    <WrapPanel>
                        <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="LOLButtonStyle">
                            <Button
                                Width="150"
                                Height="40"
                                Content="默认按钮"
                                Style="{StaticResource LOLButtonStyle}" />
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="LOLPrimaryButtonStyle">
                            <Button
                                Width="150"
                                Height="40"
                                Content="主要按钮"
                                Style="{StaticResource LOLPrimaryButtonStyle}" />
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="LOLSecondaryButtonStyle">
                            <Button
                                Width="150"
                                Height="40"
                                Content="次要按钮"
                                Style="{StaticResource LOLSecondaryButtonStyle}" />
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="LOLSuccessButtonStyle">
                            <Button
                                Width="150"
                                Height="40"
                                Content="成功按钮"
                                Style="{StaticResource LOLSuccessButtonStyle}" />
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="LOLDangerButtonStyle">
                            <Button
                                Width="150"
                                Height="40"
                                Content="危险按钮"
                                Style="{StaticResource LOLDangerButtonStyle}" />
                        </smtx:XamlDisplay>
                    </WrapPanel>

                </StackPanel>
            </Border>

            <!--  不同尺寸  -->
            <Border
                Margin="0,0,0,20"
                Padding="20"
                Background="{DynamicResource BackgroundMediumBrush}"
                BorderBrush="{DynamicResource BorderDarkBrush}"
                BorderThickness="0.5">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,15"
                        FontSize="{StaticResource FontSizeLarge}"
                        FontWeight="{StaticResource FontWeightBold}"
                        Foreground="{DynamicResource PrimaryGoldBrush}"
                        Text="不同尺寸" />

                    <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="ButtonSizes">
                        <WrapPanel>
                            <Button
                                Width="100"
                                Height="30"
                                Margin="0,0,15,0"
                                Content="小按钮"
                                FontSize="{DynamicResource FontSizeSmall}"
                                Style="{StaticResource LOLButtonStyle}" />
                            <Button
                                Width="150"
                                Height="40"
                                Margin="0,0,15,0"
                                Content="中等按钮"
                                Style="{StaticResource LOLButtonStyle}" />
                            <Button
                                Width="200"
                                Height="50"
                                Margin="0,0,15,0"
                                Content="大按钮"
                                FontSize="{StaticResource FontSizeLarge}"
                                Style="{StaticResource LOLButtonStyle}" />
                        </WrapPanel>
                    </smtx:XamlDisplay>
                </StackPanel>
            </Border>

            <!--  不同状态  -->
            <Border
                Margin="0,0,0,20"
                Padding="20"
                Background="{DynamicResource BackgroundMediumBrush}"
                BorderBrush="{DynamicResource BorderDarkBrush}"
                BorderThickness="0.5">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,15"
                        FontSize="{StaticResource FontSizeLarge}"
                        FontWeight="{StaticResource FontWeightBold}"
                        Foreground="{DynamicResource PrimaryGoldBrush}"
                        Text="不同状态" />

                    <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="ButtonStates">
                        <WrapPanel>
                            <Button
                                Width="150"
                                Height="40"
                                Margin="0,0,15,0"
                                Content="正常状态"
                                Style="{StaticResource LOLButtonStyle}" />
                            <Button
                                Width="150"
                                Height="40"
                                Content="禁用状态"
                                IsEnabled="False"
                                Style="{StaticResource LOLButtonStyle}" />
                        </WrapPanel>
                    </smtx:XamlDisplay>
                </StackPanel>
            </Border>

            <!--  带图标的按钮  -->
            <Border
                Margin="0,0,0,20"
                Padding="20"
                Background="{DynamicResource BackgroundMediumBrush}"
                BorderBrush="{DynamicResource BorderDarkBrush}"
                BorderThickness="0.5">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,15"
                        FontSize="{StaticResource FontSizeLarge}"
                        FontWeight="{StaticResource FontWeightBold}"
                        Foreground="{DynamicResource PrimaryGoldBrush}"
                        Text="带图标的按钮" />

                    <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="ButtonWithIcon">
                        <WrapPanel>
                            <Button
                                Width="150"
                                Height="40"
                                Margin="0,0,15,0"
                                Style="{StaticResource LOLButtonStyle}">
                                <StackPanel
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Orientation="Horizontal">
                                    <TextBlock
                                        Margin="0,0,5,0"
                                        FontSize="16"
                                        Text="⚔" />
                                    <TextBlock Text="攻击" />
                                </StackPanel>
                            </Button>
                            <Button
                                Width="150"
                                Height="40"
                                Margin="0,0,15,0"
                                Style="{StaticResource LOLPrimaryButtonStyle}">
                                <StackPanel
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Orientation="Horizontal">
                                    <TextBlock
                                        Margin="0,0,5,0"
                                        FontSize="16"
                                        Text="💾" />
                                    <TextBlock Text="保存" />
                                </StackPanel>
                            </Button>
                            <Button
                                Width="150"
                                Height="40"
                                Margin="0,0,15,0"
                                Style="{StaticResource LOLSuccessButtonStyle}">
                                <StackPanel
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Orientation="Horizontal">
                                    <TextBlock
                                        Margin="0,0,5,0"
                                        FontSize="16"
                                        Text="✅" />
                                    <TextBlock Text="完成" />
                                </StackPanel>
                            </Button>
                            <Button
                                Width="150"
                                Height="40"
                                Style="{StaticResource LOLDangerButtonStyle}">
                                <StackPanel
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Orientation="Horizontal">
                                    <TextBlock
                                        Margin="0,0,5,0"
                                        FontSize="16"
                                        Text="🗑" />
                                    <TextBlock Text="删除" />
                                </StackPanel>
                            </Button>
                        </WrapPanel>
                    </smtx:XamlDisplay>
                </StackPanel>
            </Border>

            <!--  加载状态按钮  -->
            <Border
                Margin="0,0,0,20"
                Padding="20"
                Background="{DynamicResource BackgroundMediumBrush}"
                BorderBrush="{DynamicResource BorderDarkBrush}"
                BorderThickness="0.5">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,15"
                        FontSize="{StaticResource FontSizeLarge}"
                        FontWeight="{StaticResource FontWeightBold}"
                        Foreground="{DynamicResource PrimaryGoldBrush}"
                        Text="加载状态" />

                    <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="ButtonLoading">
                        <WrapPanel>
                            <Button
                                Width="150"
                                Height="40"
                                Margin="0,0,15,0"
                                Style="{StaticResource LOLButtonStyle}">
                                <StackPanel
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Orientation="Horizontal">
                                    <TextBlock
                                        Margin="0,0,5,0"
                                        FontSize="16"
                                        Text="⏳" />
                                    <TextBlock Text="加载中..." />
                                </StackPanel>
                            </Button>
                            <Button
                                Width="150"
                                Height="40"
                                Margin="0,0,15,0"
                                IsEnabled="False"
                                Style="{StaticResource LOLPrimaryButtonStyle}">
                                <StackPanel
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Orientation="Horizontal">
                                    <TextBlock
                                        Margin="0,0,5,0"
                                        FontSize="16"
                                        Text="⏳" />
                                    <TextBlock Text="提交中..." />
                                </StackPanel>
                            </Button>
                        </WrapPanel>
                    </smtx:XamlDisplay>
                </StackPanel>
            </Border>

            <!--  轮廓按钮  -->
            <Border
                Margin="0,0,0,20"
                Padding="20"
                Background="{DynamicResource BackgroundMediumBrush}"
                BorderBrush="{DynamicResource BorderDarkBrush}"
                BorderThickness="0.5">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,15"
                        FontSize="{StaticResource FontSizeLarge}"
                        FontWeight="{StaticResource FontWeightBold}"
                        Foreground="{DynamicResource PrimaryGoldBrush}"
                        Text="轮廓按钮" />

                    <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="ButtonOutline">
                        <WrapPanel>
                            <Button
                                Width="150"
                                Height="40"
                                Margin="0,0,15,0"
                                Background="Transparent"
                                BorderBrush="{DynamicResource PrimaryGoldBrush}"
                                BorderThickness="1"
                                Content="轮廓按钮"
                                Style="{StaticResource LOLButtonStyle}" />
                            <Button
                                Width="150"
                                Height="40"
                                Margin="0,0,15,0"
                                Background="Transparent"
                                BorderBrush="{DynamicResource SecondaryBlueBrush}"
                                BorderThickness="1"
                                Content="轮廓按钮"
                                Style="{StaticResource LOLSecondaryButtonStyle}" />
                            <Button
                                Width="150"
                                Height="40"
                                Background="Transparent"
                                BorderBrush="{DynamicResource SuccessGreenBrush}"
                                BorderThickness="1"
                                Content="轮廓按钮"
                                Style="{StaticResource LOLSuccessButtonStyle}" />
                        </WrapPanel>
                    </smtx:XamlDisplay>
                </StackPanel>
            </Border>

            <!--  圆角按钮  -->
            <Border
                Margin="0,0,0,20"
                Padding="20"
                Background="{DynamicResource BackgroundMediumBrush}"
                BorderBrush="{DynamicResource BorderDarkBrush}"
                BorderThickness="0.5">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,15"
                        FontSize="{StaticResource FontSizeLarge}"
                        FontWeight="{StaticResource FontWeightBold}"
                        Foreground="{DynamicResource PrimaryGoldBrush}"
                        Text="圆角按钮" />

                    <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="ButtonRounded">
                        <WrapPanel>
                            <Button
                                Width="120"
                                Height="40"
                                Margin="0,0,15,0"
                                Content="圆角 4"
                                Style="{StaticResource LOLButtonStyle}">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border
                                            Padding="10,8"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="4">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                            <Button
                                Width="120"
                                Height="40"
                                Margin="0,0,15,0"
                                Content="圆角 8"
                                Style="{StaticResource LOLButtonStyle}">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border
                                            Padding="10,8"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                            <Button
                                Width="120"
                                Height="40"
                                Content="圆角 20"
                                Style="{StaticResource LOLButtonStyle}">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border
                                            Padding="10,8"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="20">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </WrapPanel>
                    </smtx:XamlDisplay>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>

