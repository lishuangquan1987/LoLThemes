<UserControl x:Class="LOLThemes.Wpf.Samples.Views.CheckBoxShowcaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:smtx="clr-namespace:ShowMeTheXAML;assembly=ShowMeTheXAML"
             Background="{DynamicResource BackgroundDarkBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="30">
        <StackPanel>
            <!-- 标题 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderGoldBrush}"
                   BorderThickness="0,0,0,2"
                   Padding="20,15"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="CheckBox - 复选框" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,8"/>
                    <TextBlock Text="复选框用于允许用户选择一个或多个选项。" 
                          FontSize="{StaticResource FontSizeNormal}"
                          Foreground="{DynamicResource TextSecondaryBrush}"/>
                </StackPanel>
            </Border>
            
            <!-- 基础用法 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="基础用法" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <StackPanel>
                        <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="CheckBoxOption1">
                            <CheckBox Content="选项 1"/>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="CheckBoxOption2">
                            <CheckBox Content="选项 2" IsChecked="True"/>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="CheckBoxOption3">
                            <CheckBox Content="选项 3"/>
                        </smtx:XamlDisplay>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- 不同状态 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="不同状态" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <StackPanel>
                        <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="CheckBoxUnchecked">
                            <CheckBox Content="未选中"/>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="CheckBoxChecked">
                            <CheckBox Content="已选中" IsChecked="True"/>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="CheckBoxDisabled">
                            <CheckBox Content="禁用状态" IsEnabled="False"/>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="CheckBoxDisabledChecked">
                            <CheckBox Content="禁用已选中" IsChecked="True" IsEnabled="False"/>
                        </smtx:XamlDisplay>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- 横向布局 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="横向布局" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <WrapPanel>
                        <smtx:XamlDisplay Margin="0,0,20,0" UniqueKey="CheckBoxHorizontal1">
                            <CheckBox Content="选项 1"/>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,20,0" UniqueKey="CheckBoxHorizontal2">
                            <CheckBox Content="选项 2" IsChecked="True"/>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,20,0" UniqueKey="CheckBoxHorizontal3">
                            <CheckBox Content="选项 3"/>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,20,0" UniqueKey="CheckBoxHorizontal4">
                            <CheckBox Content="选项 4"/>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="CheckBoxHorizontal5">
                            <CheckBox Content="选项 5"/>
                        </smtx:XamlDisplay>
                    </WrapPanel>
                </StackPanel>
            </Border>
            
            <!-- 带图标的复选框 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="带图标的复选框" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <StackPanel>
                        <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="CheckBoxSearch">
                            <CheckBox>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="🔍" FontSize="18" Margin="0,0,8,0"/>
                                    <TextBlock Text="搜索功能"/>
                                </StackPanel>
                            </CheckBox>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="CheckBoxEmail">
                            <CheckBox IsChecked="True">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="📧" FontSize="18" Margin="0,0,8,0"/>
                                    <TextBlock Text="邮件通知"/>
                                </StackPanel>
                            </CheckBox>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="CheckBoxNotification">
                            <CheckBox>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="🔔" FontSize="18" Margin="0,0,8,0"/>
                                    <TextBlock Text="推送通知"/>
                                </StackPanel>
                            </CheckBox>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="CheckBoxPrivacy">
                            <CheckBox IsChecked="True">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="🔒" FontSize="18" Margin="0,0,8,0"/>
                                    <TextBlock Text="隐私保护"/>
                                </StackPanel>
                            </CheckBox>
                        </smtx:XamlDisplay>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- 复选框组 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="复选框组" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <StackPanel>
                        <Border Background="{DynamicResource BackgroundLightBrush}"
                                BorderBrush="{DynamicResource BorderDarkBrush}"
                                BorderThickness="0.5"
                                Padding="15"
                                Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock Text="游戏设置" 
                                          FontSize="{StaticResource FontSizeMedium}"
                                          FontWeight="{StaticResource FontWeightBold}"
                                          Foreground="{DynamicResource PrimaryGoldBrush}"
                                          Margin="0,0,0,10"/>
                                <StackPanel>
                                    <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="CheckBoxMusic">
                                        <CheckBox Content="音乐" IsChecked="True"/>
                                    </smtx:XamlDisplay>
                                    <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="CheckBoxSound">
                                        <CheckBox Content="音效" IsChecked="True"/>
                                    </smtx:XamlDisplay>
                                    <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="CheckBoxVoice">
                                        <CheckBox Content="语音"/>
                                    </smtx:XamlDisplay>
                                    <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="CheckBoxVibration">
                                        <CheckBox Content="震动"/>
                                    </smtx:XamlDisplay>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                        <Border Background="{DynamicResource BackgroundLightBrush}"
                                BorderBrush="{DynamicResource BorderDarkBrush}"
                                BorderThickness="0.5"
                                Padding="15" IsEnabled="False">
                            <StackPanel>
                                <TextBlock Text="高级设置" 
                                          FontSize="{StaticResource FontSizeMedium}"
                                          FontWeight="{StaticResource FontWeightBold}"
                                          Foreground="{DynamicResource PrimaryGoldBrush}"
                                          Margin="0,0,0,10"/>
                                <StackPanel>
                                    <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="CheckBoxFrameRate">
                                        <CheckBox Content="帧率限制"/>
                                    </smtx:XamlDisplay>
                                    <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="CheckBoxVSync">
                                        <CheckBox Content="垂直同步" IsChecked="True"/>
                                    </smtx:XamlDisplay>
                                    <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="CheckBoxAntiAliasing">
                                        <CheckBox Content="抗锯齿"/>
                                    </smtx:XamlDisplay>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- 复选框带描述 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="复选框带描述" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <StackPanel>
                        <smtx:XamlDisplay Margin="0,0,0,15" UniqueKey="CheckBoxRememberMe">
                            <CheckBox>
                                <StackPanel>
                                    <TextBlock Text="记住我" 
                                              FontSize="{StaticResource FontSizeNormal}"
                                              FontWeight="{StaticResource FontWeightMedium}"
                                              Foreground="{DynamicResource TextPrimaryBrush}"
                                              Margin="0,0,0,5"/>
                                    <TextBlock Text="下次自动登录，无需再次输入密码" 
                                              FontSize="{DynamicResource FontSizeSmall}"
                                              Foreground="{DynamicResource TextSecondaryBrush}"
                                              TextWrapping="Wrap"/>
                                </StackPanel>
                            </CheckBox>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,15" UniqueKey="CheckBoxTerms">
                            <CheckBox IsChecked="True">
                                <StackPanel>
                                    <TextBlock Text="接受服务条款" 
                                              FontSize="{StaticResource FontSizeNormal}"
                                              FontWeight="{StaticResource FontWeightMedium}"
                                              Foreground="{DynamicResource TextPrimaryBrush}"
                                              Margin="0,0,0,5"/>
                                    <TextBlock Text="我已阅读并同意用户协议和隐私政策" 
                                              FontSize="{DynamicResource FontSizeSmall}"
                                              Foreground="{DynamicResource TextSecondaryBrush}"
                                              TextWrapping="Wrap"/>
                                </StackPanel>
                            </CheckBox>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="CheckBoxSubscribe">
                            <CheckBox>
                                <StackPanel>
                                    <TextBlock Text="订阅邮件通知" 
                                              FontSize="{StaticResource FontSizeNormal}"
                                              FontWeight="{StaticResource FontWeightMedium}"
                                              Foreground="{DynamicResource TextPrimaryBrush}"
                                              Margin="0,0,0,5"/>
                                    <TextBlock Text="接收产品更新、优惠活动等邮件推送" 
                                              FontSize="{DynamicResource FontSizeSmall}"
                                              Foreground="{DynamicResource TextSecondaryBrush}"
                                              TextWrapping="Wrap"/>
                                </StackPanel>
                            </CheckBox>
                        </smtx:XamlDisplay>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- 不确定状态复选框 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="不确定状态复选框" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <StackPanel>
                        <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="CheckBoxIndeterminateState">
                            <CheckBox Content="全选" IsChecked="{x:Null}"/>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="CheckBoxTrueState">
                            <CheckBox Content="选中" IsChecked="True"/>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="CheckBoxFalseState">
                            <CheckBox Content="未选中" IsChecked="False"/>
                        </smtx:XamlDisplay>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
