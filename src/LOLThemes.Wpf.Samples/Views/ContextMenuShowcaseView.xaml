<UserControl x:Class="LOLThemes.Wpf.Samples.Views.ContextMenuShowcaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:smtx="clr-namespace:ShowMeTheXAML;assembly=ShowMeTheXAML"
             Background="{DynamicResource BackgroundDarkBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="30">
        <StackPanel>
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderGoldBrush}"
                   BorderThickness="0,0,0,2"
                   Padding="20,15"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="ContextMenu - 上下文菜单" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,8"/>
                    <TextBlock Text="上下文菜单用于在右键点击时显示操作选项。" 
                          FontSize="{StaticResource FontSizeNormal}"
                          Foreground="{DynamicResource TextSecondaryBrush}"/>
                </StackPanel>
            </Border>
            
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="基础用法" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <smtx:XamlDisplay UniqueKey="ContextMenuBasic" Margin="0,0,0,0">
                        <StackPanel Width="300" HorizontalAlignment="Left">
                            <TextBlock Text="右键点击我查看上下文菜单" 
                                     Foreground="{DynamicResource TextPrimaryBrush}"
                                     Padding="10"
                                     Background="{DynamicResource BackgroundLightBrush}"
                                     Margin="0,0,0,15">
                                <TextBlock.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="复制"/>
                                        <MenuItem Header="粘贴"/>
                                        <Separator/>
                                        <MenuItem Header="删除"/>
                                    </ContextMenu>
                                </TextBlock.ContextMenu>
                            </TextBlock>
                            <Button Content="右键点击按钮" Width="200" Height="40">
                                <Button.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="编辑"/>
                                        <MenuItem Header="删除"/>
                                    </ContextMenu>
                                </Button.ContextMenu>
                            </Button>
                        </StackPanel>
                    </smtx:XamlDisplay>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>

