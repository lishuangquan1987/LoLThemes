<UserControl x:Class="LOLThemes.Wpf.Samples.Views.ControlShowcaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LOLThemes.Wpf.Samples.Views"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1150"
             Background="{StaticResource BackgroundDarkBrush}">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto" 
                  HorizontalScrollBarVisibility="Disabled"
                  Padding="40">
        <StackPanel>
            <!-- 页面标题 -->
            <TextBlock Text="LOL 风格控件展示" 
                      FontSize="{StaticResource FontSizeXXLarge}"
                      FontWeight="{StaticResource FontWeightBold}"
                      Foreground="{StaticResource PrimaryGoldBrush}"
                      Margin="0,0,0,20"/>
            
            <TextBlock Text="本页面展示 LOLThemes.Wpf 控件库中所有可用的控件样式" 
                      FontSize="{StaticResource FontSizeNormal}"
                      Foreground="{StaticResource TextSecondaryBrush}"
                      Margin="0,0,0,40"/>
            
            <!-- Button 控件组 -->
            <Border Background="{StaticResource BackgroundMediumBrush}"
                   BorderBrush="{StaticResource BorderDarkBrush}"
                   BorderThickness="1"
                   Padding="30"
                   Margin="0,0,0,30">
                <StackPanel>
                    <TextBlock Text="Button - 按钮" 
                              FontSize="{StaticResource FontSizeLarge}"
                              FontWeight="{StaticResource FontWeightBold}"
                              Foreground="{StaticResource TextPrimaryBrush}"
                              Margin="0,0,0,20"/>
                    
                    <TextBlock Text="基础按钮样式" 
                              FontSize="{StaticResource FontSizeNormal}"
                              Foreground="{StaticResource TextSecondaryBrush}"
                              Margin="0,0,0,10"/>
                    
                    <WrapPanel Margin="0,0,0,20">
                        <Button Content="默认按钮" 
                               Style="{StaticResource LOLButtonStyle}"
                               Width="150" Height="40" Margin="0,0,15,0"/>
                        <Button Content="主要按钮" 
                               Style="{StaticResource LOLPrimaryButtonStyle}"
                               Width="150" Height="40" Margin="0,0,15,0"/>
                        <Button Content="次要按钮" 
                               Style="{StaticResource LOLSecondaryButtonStyle}"
                               Width="150" Height="40" Margin="0,0,15,0"/>
                        <Button Content="成功按钮" 
                               Style="{StaticResource LOLSuccessButtonStyle}"
                               Width="150" Height="40" Margin="0,0,15,0"/>
                        <Button Content="危险按钮" 
                               Style="{StaticResource LOLDangerButtonStyle}"
                               Width="150" Height="40" Margin="0,0,15,0"/>
                    </WrapPanel>
                    
                    <TextBlock Text="不同尺寸" 
                              FontSize="{StaticResource FontSizeNormal}"
                              Foreground="{StaticResource TextSecondaryBrush}"
                              Margin="0,0,0,10"/>
                    
                    <WrapPanel Margin="0,0,0,20">
                        <Button Content="小按钮" 
                               Style="{StaticResource LOLButtonStyle}"
                               FontSize="{StaticResource FontSizeSmall}"
                               Width="100" Height="30" Margin="0,0,15,0"/>
                        <Button Content="中等按钮" 
                               Style="{StaticResource LOLButtonStyle}"
                               Width="150" Height="40" Margin="0,0,15,0"/>
                        <Button Content="大按钮" 
                               Style="{StaticResource LOLButtonStyle}"
                               FontSize="{StaticResource FontSizeLarge}"
                               Width="200" Height="50" Margin="0,0,15,0"/>
                    </WrapPanel>
                    
                    <TextBlock Text="禁用状态" 
                              FontSize="{StaticResource FontSizeNormal}"
                              Foreground="{StaticResource TextSecondaryBrush}"
                              Margin="0,0,0,10"/>
                    
                    <WrapPanel>
                        <Button Content="禁用按钮" 
                               Style="{StaticResource LOLButtonStyle}"
                               IsEnabled="False"
                               Width="150" Height="40" Margin="0,0,15,0"/>
                        <Button Content="禁用主要按钮" 
                               Style="{StaticResource LOLPrimaryButtonStyle}"
                               IsEnabled="False"
                               Width="150" Height="40" Margin="0,0,15,0"/>
                    </WrapPanel>
                </StackPanel>
            </Border>


            <!-- TextBox 控件组 -->
            <Border Background="{StaticResource BackgroundMediumBrush}"
                   BorderBrush="{StaticResource BorderDarkBrush}"
                   BorderThickness="1"
                   Padding="30"
                   Margin="0,0,0,30">
                <StackPanel>
                    <TextBlock Text="TextBox - 文本输入框" 
                              FontSize="{StaticResource FontSizeLarge}"
                              FontWeight="{StaticResource FontWeightBold}"
                              Foreground="{StaticResource TextPrimaryBrush}"
                              Margin="0,0,0,20"/>
                    
                    <TextBlock Text="基础文本框" 
                              FontSize="{StaticResource FontSizeNormal}"
                              Foreground="{StaticResource TextSecondaryBrush}"
                              Margin="0,0,0,10"/>
                    
                    <TextBox Style="{StaticResource LOLTextBoxStyle}"
                            Tag="请输入用户名..."
                            Width="300" Height="40" 
                            HorizontalAlignment="Left"
                            Margin="0,0,0,15"/>
                    
                    <TextBox Style="{StaticResource LOLTextBoxStyle}"
                            Text="已填充的文本框"
                            Width="300" Height="40" 
                            HorizontalAlignment="Left"
                            Margin="0,0,0,15"/>
                    
                    <TextBox Style="{StaticResource LOLTextBoxStyle}"
                            Tag="禁用状态..."
                            IsEnabled="False"
                            Width="300" Height="40" 
                            HorizontalAlignment="Left"
                            Margin="0,0,0,15"/>
                    
                    <TextBlock Text="多行文本框" 
                              FontSize="{StaticResource FontSizeNormal}"
                              Foreground="{StaticResource TextSecondaryBrush}"
                              Margin="0,15,0,10"/>
                    
                    <TextBox Style="{StaticResource LOLTextBoxStyle}"
                            Tag="请输入多行文本..."
                            TextWrapping="Wrap"
                            AcceptsReturn="True"
                            VerticalScrollBarVisibility="Auto"
                            Width="300" Height="100" 
                            HorizontalAlignment="Left"/>
                </StackPanel>
            </Border>
            
            <!-- PasswordBox 控件组 -->
            <Border Background="{StaticResource BackgroundMediumBrush}"
                   BorderBrush="{StaticResource BorderDarkBrush}"
                   BorderThickness="1"
                   Padding="30"
                   Margin="0,0,0,30">
                <StackPanel>
                    <TextBlock Text="PasswordBox - 密码输入框" 
                              FontSize="{StaticResource FontSizeLarge}"
                              FontWeight="{StaticResource FontWeightBold}"
                              Foreground="{StaticResource TextPrimaryBrush}"
                              Margin="0,0,0,20"/>
                    
                    <PasswordBox Style="{StaticResource LOLPasswordBoxStyle}"
                                Width="300" Height="40" 
                                HorizontalAlignment="Left"
                                Margin="0,0,0,15"/>
                    
                    <PasswordBox Style="{StaticResource LOLPasswordBoxStyle}"
                                Password="password123"
                                Width="300" Height="40" 
                                HorizontalAlignment="Left"
                                Margin="0,0,0,15"/>
                    
                    <PasswordBox Style="{StaticResource LOLPasswordBoxStyle}"
                                IsEnabled="False"
                                Width="300" Height="40" 
                                HorizontalAlignment="Left"/>
                </StackPanel>
            </Border>
            
            <!-- ComboBox 控件组 -->
            <Border Background="{StaticResource BackgroundMediumBrush}"
                   BorderBrush="{StaticResource BorderDarkBrush}"
                   BorderThickness="1"
                   Padding="30"
                   Margin="0,0,0,30">
                <StackPanel>
                    <TextBlock Text="ComboBox - 组合框/下拉列表" 
                              FontSize="{StaticResource FontSizeLarge}"
                              FontWeight="{StaticResource FontWeightBold}"
                              Foreground="{StaticResource TextPrimaryBrush}"
                              Margin="0,0,0,20"/>
                    
                    <TextBlock Text="基础组合框" 
                              FontSize="{StaticResource FontSizeNormal}"
                              Foreground="{StaticResource TextSecondaryBrush}"
                              Margin="0,0,0,10"/>
                    
                    <ComboBox Style="{StaticResource LOLComboBoxStyle}"
                             Width="300" Height="40" 
                             HorizontalAlignment="Left"
                             Margin="0,0,0,15">
                        <ComboBoxItem Content="选项 1"/>
                        <ComboBoxItem Content="选项 2"/>
                        <ComboBoxItem Content="选项 3"/>
                        <ComboBoxItem Content="选项 4"/>
                        <ComboBoxItem Content="选项 5"/>
                    </ComboBox>
                    
                    <ComboBox Style="{StaticResource LOLComboBoxStyle}"
                             Width="300" Height="40" 
                             HorizontalAlignment="Left"
                             SelectedIndex="1"
                             Margin="0,0,0,15">
                        <ComboBoxItem Content="已选中项 1"/>
                        <ComboBoxItem Content="已选中项 2"/>
                        <ComboBoxItem Content="已选中项 3"/>
                    </ComboBox>
                    
                    <ComboBox Style="{StaticResource LOLComboBoxStyle}"
                             Width="300" Height="40" 
                             HorizontalAlignment="Left"
                             IsEnabled="False">
                        <ComboBoxItem Content="禁用状态"/>
                    </ComboBox>
                </StackPanel>
            </Border>
            
            <!-- 说明文本 -->
            <Border Background="{StaticResource BackgroundPanelBrush}"
                   BorderBrush="{StaticResource PrimaryGoldBrush}"
                   BorderThickness="2"
                   Padding="20"
                   Margin="0,20,0,0">
                <StackPanel>
                    <TextBlock Text="🎮 更多控件正在开发中..." 
                              FontSize="{StaticResource FontSizeMedium}"
                              FontWeight="{StaticResource FontWeightBold}"
                              Foreground="{StaticResource PrimaryGoldBrush}"
                              Margin="0,0,0,10"/>
                    <TextBlock TextWrapping="Wrap"
                              FontSize="{StaticResource FontSizeNormal}"
                              Foreground="{StaticResource TextSecondaryBrush}">
                        <Run Text="本控件库正在持续开发中，将陆续添加更多控件样式，包括："/>
                        <LineBreak/>
                        <Run Text="• CheckBox、RadioButton、ToggleButton 等选择控件"/>
                        <LineBreak/>
                        <Run Text="• ListBox、ListView、DataGrid 等列表控件"/>
                        <LineBreak/>
                        <Run Text="• ProgressBar、Slider、ScrollBar 等进度控件"/>
                        <LineBreak/>
                        <Run Text="• TabControl、GroupBox、Expander 等容器控件"/>
                        <LineBreak/>
                        <Run Text="• Menu、ContextMenu、ToolTip 等菜单控件"/>
                        <LineBreak/>
                        <Run Text="• 以及 HexagonButton、GlowButton、ChampionCard 等自定义控件"/>
                    </TextBlock>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
