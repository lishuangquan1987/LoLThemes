<UserControl x:Class="LOLThemes.Wpf.Samples.Views.RadioButtonShowcaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:LOLThemes.Wpf.Controls;assembly=LOLThemes.Wpf"
             xmlns:smtx="clr-namespace:ShowMeTheXAML;assembly=ShowMeTheXAML"
             Background="{DynamicResource BackgroundDarkBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="30">
        <StackPanel>
            <!-- 标题 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderGoldBrush}"
                   BorderThickness="0,0,0,2"
                   Padding="20,15"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="RadioButton - 单选按钮" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,8"/>
                    <TextBlock Text="单选按钮用于从一组互斥的选项中选择一个选项。" 
                          FontSize="{StaticResource FontSizeNormal}"
                          Foreground="{DynamicResource TextSecondaryBrush}"/>
                </StackPanel>
            </Border>
            
            <!-- 基础用法 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="基础用法" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <StackPanel>
                        <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="RadioButtonOption1">
                            <RadioButton Content="选项 1" 
                                       GroupName="Group1"/>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="RadioButtonOption2">
                            <RadioButton Content="选项 2" 
                                       GroupName="Group1"
                                       IsChecked="True"/>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="RadioButtonOption3">
                            <RadioButton Content="选项 3" 
                                       GroupName="Group1"/>
                        </smtx:XamlDisplay>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- 不同状态 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="不同状态" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <StackPanel>
                        <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="RadioButtonUnchecked">
                            <RadioButton Content="未选中" 
                                       GroupName="Group2"/>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="RadioButtonChecked">
                            <RadioButton Content="已选中" 
                                       GroupName="Group2"
                                       IsChecked="True"/>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="RadioButtonDisabled">
                            <RadioButton Content="禁用状态" 
                                       GroupName="Group2"
                                       IsEnabled="False"/>
                        </smtx:XamlDisplay>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- 横向布局 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="横向布局" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <WrapPanel>
                        <smtx:XamlDisplay Margin="0,0,20,0" UniqueKey="RadioButtonHorizontal1">
                            <RadioButton Content="选项 1" 
                                       GroupName="Group3"/>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,20,0" UniqueKey="RadioButtonHorizontal2">
                            <RadioButton Content="选项 2" 
                                       GroupName="Group3"
                                       IsChecked="True"/>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,20,0" UniqueKey="RadioButtonHorizontal3">
                            <RadioButton Content="选项 3" 
                                       GroupName="Group3"/>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="RadioButtonHorizontal4">
                            <RadioButton Content="选项 4" 
                                       GroupName="Group3"/>
                        </smtx:XamlDisplay>
                    </WrapPanel>
                </StackPanel>
            </Border>
            
            <!-- 带图标的单选按钮 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="带图标的单选按钮" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <StackPanel>
                        <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="RadioButtonPCVersion">
                            <RadioButton GroupName="Group4">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="🖥️" FontSize="18" Margin="0,0,8,0"/>
                                    <TextBlock Text="PC版"/>
                                </StackPanel>
                            </RadioButton>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="RadioButtonMobileVersion">
                            <RadioButton GroupName="Group4" IsChecked="True">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="📱" FontSize="18" Margin="0,0,8,0"/>
                                    <TextBlock Text="手机版"/>
                                </StackPanel>
                            </RadioButton>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="RadioButtonConsoleVersion">
                            <RadioButton GroupName="Group4">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="🎮" FontSize="18" Margin="0,0,8,0"/>
                                    <TextBlock Text="主机版"/>
                                </StackPanel>
                            </RadioButton>
                        </smtx:XamlDisplay>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- 单选按钮组 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="单选按钮组" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <StackPanel>
                        <Border Background="{DynamicResource BackgroundLightBrush}"
                                BorderBrush="{DynamicResource BorderDarkBrush}"
                                BorderThickness="0.5"
                                Padding="15" Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock Text="游戏难度" 
                                          FontSize="{StaticResource FontSizeMedium}"
                                          FontWeight="{StaticResource FontWeightBold}"
                                          Foreground="{DynamicResource PrimaryGoldBrush}"
                                          Margin="0,0,0,10"/>
                                <StackPanel>
                                    <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="RadioButtonEasy">
                                        <RadioButton Content="简单" 
                                                   GroupName="Difficulty"/>
                                    </smtx:XamlDisplay>
                                    <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="RadioButtonNormal">
                                        <RadioButton Content="普通" 
                                                   GroupName="Difficulty"
                                                   IsChecked="True"/>
                                    </smtx:XamlDisplay>
                                    <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="RadioButtonHard">
                                        <RadioButton Content="困难" 
                                                   GroupName="Difficulty"/>
                                    </smtx:XamlDisplay>
                                    <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="RadioButtonExpert">
                                        <RadioButton Content="专家" 
                                                   GroupName="Difficulty"/>
                                    </smtx:XamlDisplay>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <Border Background="{DynamicResource BackgroundLightBrush}"
                                BorderBrush="{DynamicResource BorderDarkBrush}"
                                BorderThickness="0.5"
                                Padding="15" IsEnabled="False">
                            <StackPanel>
                                <TextBlock Text="画质设置" 
                                          FontSize="{StaticResource FontSizeMedium}"
                                          FontWeight="{StaticResource FontWeightBold}"
                                          Foreground="{DynamicResource PrimaryGoldBrush}"
                                          Margin="0,0,0,10"/>
                                <StackPanel>
                                    <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="RadioButtonQualityLow">
                                        <RadioButton Content="低" 
                                                   GroupName="Quality"/>
                                    </smtx:XamlDisplay>
                                    <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="RadioButtonQualityMedium">
                                        <RadioButton Content="中" 
                                                   GroupName="Quality"/>
                                    </smtx:XamlDisplay>
                                    <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="RadioButtonQualityHigh">
                                        <RadioButton Content="高" 
                                                   GroupName="Quality"
                                                   IsChecked="True"/>
                                    </smtx:XamlDisplay>
                                    <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="RadioButtonQualityUltra">
                                        <RadioButton Content="极致" 
                                                   GroupName="Quality"/>
                                    </smtx:XamlDisplay>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
