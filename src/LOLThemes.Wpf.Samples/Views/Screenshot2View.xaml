<UserControl
    x:Class="LOLThemes.Wpf.Samples.Views.Screenshot2View"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:LOLThemes.Wpf.Controls;assembly=LOLThemes.Wpf"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="800"
    d:DesignWidth="1150"
    Background="{DynamicResource BackgroundDarkBrush}"
    mc:Ignorable="d">

    <Grid>
        <!--  主容器  -->
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  顶部标题栏  -->
            <Border
                Grid.Row="0"
                Padding="20,15"
                Background="{DynamicResource BackgroundMediumBrush}"
                BorderBrush="{DynamicResource BorderGoldBrush}"
                BorderThickness="0,0,0,2">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!--  左侧标题  -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock
                            Margin="0,0,15,0"
                            VerticalAlignment="Center"
                            FontSize="{DynamicResource FontSizeLarge}"
                            Foreground="{DynamicResource PrimaryGoldBrush}"
                            Text="🏆" />
                        <TextBlock
                            VerticalAlignment="Center"
                            FontSize="{StaticResource FontSizeLarge}"
                            FontWeight="{StaticResource FontWeightBold}"
                            Foreground="{DynamicResource TextPrimaryBrush}"
                            Text="个人资料" />
                    </StackPanel>

                    <!--  中间导航标签  -->
                    <StackPanel
                        Grid.Column="1"
                        HorizontalAlignment="Center"
                        Orientation="Horizontal">
                        <Button
                            Width="100"
                            Height="35"
                            Margin="5,0"
                            Content="概览"
                            Style="{StaticResource LOLPrimaryButtonStyle}" />
                        <Button
                            Width="100"
                            Height="35"
                            Margin="5,0"
                            Content="英雄"
                            Style="{StaticResource LOLSecondaryButtonStyle}" />
                        <Button
                            Width="100"
                            Height="35"
                            Margin="5,0"
                            Content="收藏"
                            Style="{StaticResource LOLSecondaryButtonStyle}" />
                        <Button
                            Width="100"
                            Height="35"
                            Margin="5,0"
                            Content="战绩"
                            Style="{StaticResource LOLSecondaryButtonStyle}" />
                    </StackPanel>

                    <!--  右侧设置按钮  -->
                    <Button
                        Grid.Column="2"
                        Width="40"
                        Height="40"
                        Content="⚙"
                        FontSize="{StaticResource FontSizeLarge}"
                        Style="{StaticResource LOLSecondaryButtonStyle}" />
                </Grid>
            </Border>

            <!--  主内容区域  -->
            <Grid Grid.Row="1" Margin="0,20,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="350" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!--  左侧个人信息面板  -->
                <Border
                    Grid.Column="0"
                    Margin="0,0,15,0"
                    Padding="25"
                    Background="{DynamicResource BackgroundMediumBrush}"
                    BorderBrush="{DynamicResource BorderGoldBrush}"
                    BorderThickness="2">
                    <StackPanel>
                        <!--  召唤师头像  -->
                        <Border
                            Width="150"
                            Height="150"
                            Margin="0,0,0,20"
                            HorizontalAlignment="Center"
                            Background="{DynamicResource BackgroundLightBrush}"
                            BorderBrush="{DynamicResource PrimaryGoldBrush}"
                            BorderThickness="3"
                            CornerRadius="75">
                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontSize="72"
                                Text="👤" />
                        </Border>

                        <!--  召唤师名称  -->
                        <TextBlock
                            Margin="0,0,0,10"
                            HorizontalAlignment="Center"
                            FontSize="{DynamicResource FontSizeLarge}"
                            FontWeight="{StaticResource FontWeightBold}"
                            Foreground="{DynamicResource PrimaryGoldBrush}"
                            Text="召唤师名称" />

                        <!--  等级  -->
                        <Border
                            Margin="0,0,0,25"
                            Padding="15,8"
                            HorizontalAlignment="Center"
                            Background="{DynamicResource PrimaryCyanBrush}"
                            CornerRadius="3">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock
                                    FontSize="{StaticResource FontSizeMedium}"
                                    FontWeight="{StaticResource FontWeightBold}"
                                    Foreground="{DynamicResource BackgroundDarkBrush}"
                                    Text="等级 " />
                                <TextBlock
                                    FontSize="{StaticResource FontSizeMedium}"
                                    FontWeight="{StaticResource FontWeightBold}"
                                    Foreground="{DynamicResource BackgroundDarkBrush}"
                                    Text="{Binding PlayerData.Level}" />
                            </StackPanel>
                        </Border>

                        <!--  段位信息  -->
                        <Border
                            Margin="0,0,0,20"
                            Padding="20"
                            Background="{DynamicResource BackgroundLightBrush}"
                            BorderBrush="{DynamicResource BorderGoldBrush}"
                            BorderThickness="1">
                            <StackPanel>
                                <TextBlock
                                    Margin="0,0,0,10"
                                    HorizontalAlignment="Center"
                                    FontSize="{StaticResource FontSizeMedium}"
                                    FontWeight="{StaticResource FontWeightBold}"
                                    Foreground="{DynamicResource PrimaryGoldBrush}"
                                    Text="单双排位" />

                                <Border
                                    Width="80"
                                    Height="80"
                                    Margin="0,0,0,10"
                                    HorizontalAlignment="Center"
                                    Background="{DynamicResource PrimaryGoldBrush}"
                                    CornerRadius="40">
                                    <TextBlock
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="40"
                                        Text="💎" />
                                </Border>

                                <TextBlock
                                    Margin="0,0,0,5"
                                    HorizontalAlignment="Center"
                                    FontSize="{StaticResource FontSizeLarge}"
                                    FontWeight="{StaticResource FontWeightBold}"
                                    Foreground="{DynamicResource TextPrimaryBrush}"
                                    Text="{Binding PlayerData.Rank}" />

                                <TextBlock
                                    HorizontalAlignment="Center"
                                    FontSize="{StaticResource FontSizeNormal}"
                                    Foreground="{DynamicResource TextSecondaryBrush}"
                                    Text="67 LP" />
                            </StackPanel>
                        </Border>

                        <!--  统计信息  -->
                        <StackPanel>
                            <TextBlock
                                Margin="0,0,0,15"
                                FontSize="{StaticResource FontSizeMedium}"
                                FontWeight="{StaticResource FontWeightBold}"
                                Foreground="{DynamicResource PrimaryGoldBrush}"
                                Text="本赛季统计" />

                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock
                                    Grid.Column="0"
                                    FontSize="{StaticResource FontSizeNormal}"
                                    Foreground="{DynamicResource TextSecondaryBrush}"
                                    Text="总场次" />
                                <TextBlock
                                    Grid.Column="1"
                                    FontSize="{StaticResource FontSizeNormal}"
                                    FontWeight="{StaticResource FontWeightBold}"
                                    Foreground="{DynamicResource TextPrimaryBrush}"
                                    Text="342" />
                            </Grid>

                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock
                                    Grid.Column="0"
                                    FontSize="{StaticResource FontSizeNormal}"
                                    Foreground="{DynamicResource TextSecondaryBrush}"
                                    Text="胜率" />
                                <TextBlock
                                    Grid.Column="1"
                                    FontSize="{StaticResource FontSizeNormal}"
                                    FontWeight="{StaticResource FontWeightBold}"
                                    Foreground="{DynamicResource SuccessGreenBrush}"
                                    Text="54.7%" />
                            </Grid>

                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock
                                    Grid.Column="0"
                                    FontSize="{StaticResource FontSizeNormal}"
                                    Foreground="{DynamicResource TextSecondaryBrush}"
                                    Text="KDA" />
                                <TextBlock
                                    Grid.Column="1"
                                    FontSize="{StaticResource FontSizeNormal}"
                                    FontWeight="{StaticResource FontWeightBold}"
                                    Foreground="{DynamicResource TextPrimaryBrush}"
                                    Text="3.2:1" />
                            </Grid>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!--  右侧内容区域  -->
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!--  成就展示  -->
                    <Border
                        Grid.Row="0"
                        Margin="0,0,0,15"
                        Padding="20"
                        Background="{DynamicResource BackgroundMediumBrush}"
                        BorderBrush="{DynamicResource BorderDarkBrush}"
                        BorderThickness="1">
                        <StackPanel>
                            <TextBlock
                                Margin="0,0,0,15"
                                FontSize="{StaticResource FontSizeMedium}"
                                FontWeight="{StaticResource FontWeightBold}"
                                Foreground="{DynamicResource PrimaryGoldBrush}"
                                Text="最近成就" />

                            <WrapPanel>
                                <!--  成就徽章  -->
                                <Border
                                    Width="100"
                                    Height="100"
                                    Margin="0,0,15,0"
                                    Background="{DynamicResource BackgroundLightBrush}"
                                    BorderBrush="{DynamicResource PrimaryGoldBrush}"
                                    BorderThickness="2"
                                    Cursor="Hand"
                                    RenderTransformOrigin="0.5,0.5">
                                    <Border.RenderTransform>
                                        <ScaleTransform ScaleX="1" ScaleY="1" />
                                    </Border.RenderTransform>
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock
                                            Margin="0,0,0,5"
                                            HorizontalAlignment="Center"
                                            FontSize="40"
                                            Text="🏅" />
                                        <TextBlock
                                            HorizontalAlignment="Center"
                                            FontSize="{DynamicResource FontSizeSmall}"
                                            Foreground="{DynamicResource TextPrimaryBrush}"
                                            Text="五杀" />
                                    </StackPanel>

                                </Border>

                                <Border
                                    Width="100"
                                    Height="100"
                                    Margin="0,0,15,0"
                                    Background="{DynamicResource BackgroundLightBrush}"
                                    BorderBrush="{DynamicResource PrimaryCyanBrush}"
                                    BorderThickness="2"
                                    Cursor="Hand"
                                    RenderTransformOrigin="0.5,0.5">
                                    <Border.RenderTransform>
                                        <ScaleTransform ScaleX="1" ScaleY="1" />
                                    </Border.RenderTransform>
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock
                                            Margin="0,0,0,5"
                                            HorizontalAlignment="Center"
                                            FontSize="40"
                                            Text="⭐" />
                                        <TextBlock
                                            HorizontalAlignment="Center"
                                            FontSize="{DynamicResource FontSizeSmall}"
                                            Foreground="{DynamicResource TextPrimaryBrush}"
                                            Text="MVP" />
                                    </StackPanel>

                                </Border>

                                <Border
                                    Width="100"
                                    Height="100"
                                    Margin="0,0,15,0"
                                    Background="{DynamicResource BackgroundLightBrush}"
                                    BorderBrush="{DynamicResource PrimaryGoldBrush}"
                                    BorderThickness="2"
                                    Cursor="Hand"
                                    RenderTransformOrigin="0.5,0.5">
                                    <Border.RenderTransform>
                                        <ScaleTransform ScaleX="1" ScaleY="1" />
                                    </Border.RenderTransform>
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock
                                            Margin="0,0,0,5"
                                            HorizontalAlignment="Center"
                                            FontSize="40"
                                            Text="🎯" />
                                        <TextBlock
                                            HorizontalAlignment="Center"
                                            FontSize="{DynamicResource FontSizeSmall}"
                                            Foreground="{DynamicResource TextPrimaryBrush}"
                                            Text="神射手" />
                                    </StackPanel>

                                </Border>

                                <Border
                                    Width="100"
                                    Height="100"
                                    Background="{DynamicResource BackgroundLightBrush}"
                                    BorderBrush="{DynamicResource PrimaryCyanBrush}"
                                    BorderThickness="2"
                                    Cursor="Hand"
                                    RenderTransformOrigin="0.5,0.5">
                                    <Border.RenderTransform>
                                        <ScaleTransform ScaleX="1" ScaleY="1" />
                                    </Border.RenderTransform>
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock
                                            Margin="0,0,0,5"
                                            HorizontalAlignment="Center"
                                            FontSize="40"
                                            Text="🛡️" />
                                        <TextBlock
                                            HorizontalAlignment="Center"
                                            FontSize="{DynamicResource FontSizeSmall}"
                                            Foreground="{DynamicResource TextPrimaryBrush}"
                                            Text="守护者" />
                                    </StackPanel>

                                </Border>
                            </WrapPanel>
                        </StackPanel>
                    </Border>

                    <!--  最常使用英雄  -->
                    <Border
                        Grid.Row="1"
                        Padding="20"
                        Background="{DynamicResource BackgroundMediumBrush}"
                        BorderBrush="{DynamicResource BorderDarkBrush}"
                        BorderThickness="1">
                        <StackPanel>
                            <TextBlock
                                Margin="0,0,0,15"
                                FontSize="{StaticResource FontSizeMedium}"
                                FontWeight="{StaticResource FontWeightBold}"
                                Foreground="{DynamicResource PrimaryGoldBrush}"
                                Text="最常使用英雄" />

                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <StackPanel>
                                    <!--  英雄统计项 1  -->
                                    <Border
                                        Margin="0,0,0,10"
                                        Padding="15"
                                        Background="{DynamicResource BackgroundLightBrush}"
                                        BorderBrush="{DynamicResource BorderGoldBrush}"
                                        BorderThickness="1"
                                        Cursor="Hand">
                                        <Border.RenderTransform>
                                            <ScaleTransform ScaleX="1" ScaleY="1" />
                                        </Border.RenderTransform>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="60" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <Border
                                                Grid.Column="0"
                                                Width="50"
                                                Height="50"
                                                Background="{DynamicResource PrimaryGoldBrush}"
                                                CornerRadius="2">
                                                <TextBlock
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    FontSize="{DynamicResource FontSizeSmall}"
                                                    FontWeight="{StaticResource FontWeightBold}"
                                                    Foreground="{DynamicResource BackgroundDarkBrush}"
                                                    Text="亚索" />
                                            </Border>

                                            <StackPanel
                                                Grid.Column="1"
                                                Margin="15,0,0,0"
                                                VerticalAlignment="Center">
                                                <TextBlock
                                                    Margin="0,0,0,5"
                                                    FontSize="{StaticResource FontSizeNormal}"
                                                    FontWeight="{StaticResource FontWeightBold}"
                                                    Foreground="{DynamicResource TextPrimaryBrush}"
                                                    Text="疾风剑豪 - 亚索" />
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock
                                                        FontSize="{DynamicResource FontSizeSmall}"
                                                        Foreground="{DynamicResource TextSecondaryBrush}"
                                                        Text="87场 · " />
                                                    <TextBlock
                                                        FontSize="{DynamicResource FontSizeSmall}"
                                                        Foreground="{DynamicResource SuccessGreenBrush}"
                                                        Text="58% 胜率" />
                                                </StackPanel>
                                            </StackPanel>

                                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                                <TextBlock
                                                    HorizontalAlignment="Right"
                                                    FontSize="{StaticResource FontSizeLarge}"
                                                    FontWeight="{StaticResource FontWeightBold}"
                                                    Foreground="{DynamicResource PrimaryGoldBrush}"
                                                    Text="4.2" />
                                                <TextBlock
                                                    HorizontalAlignment="Right"
                                                    FontSize="{DynamicResource FontSizeSmall}"
                                                    Foreground="{DynamicResource TextSecondaryBrush}"
                                                    Text="KDA" />
                                            </StackPanel>
                                        </Grid>

                                    </Border>

                                    <!--  英雄统计项 2  -->
                                    <Border
                                        Margin="0,0,0,10"
                                        Padding="15"
                                        Background="{DynamicResource BackgroundLightBrush}"
                                        BorderBrush="{DynamicResource BorderDarkBrush}"
                                        BorderThickness="1"
                                        Cursor="Hand">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="60" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <Border
                                                Grid.Column="0"
                                                Width="50"
                                                Height="50"
                                                Background="{DynamicResource PrimaryCyanBrush}"
                                                CornerRadius="2">
                                                <TextBlock
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    FontSize="{DynamicResource FontSizeSmall}"
                                                    FontWeight="{StaticResource FontWeightBold}"
                                                    Foreground="{DynamicResource BackgroundDarkBrush}"
                                                    Text="劫" />
                                            </Border>

                                            <StackPanel
                                                Grid.Column="1"
                                                Margin="15,0,0,0"
                                                VerticalAlignment="Center">
                                                <TextBlock
                                                    Margin="0,0,0,5"
                                                    FontSize="{StaticResource FontSizeNormal}"
                                                    FontWeight="{StaticResource FontWeightBold}"
                                                    Foreground="{DynamicResource TextPrimaryBrush}"
                                                    Text="影流之主 - 劫" />
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock
                                                        FontSize="{DynamicResource FontSizeSmall}"
                                                        Foreground="{DynamicResource TextSecondaryBrush}"
                                                        Text="65场 · " />
                                                    <TextBlock
                                                        FontSize="{DynamicResource FontSizeSmall}"
                                                        Foreground="{DynamicResource SuccessGreenBrush}"
                                                        Text="52% 胜率" />
                                                </StackPanel>
                                            </StackPanel>

                                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                                <TextBlock
                                                    HorizontalAlignment="Right"
                                                    FontSize="{StaticResource FontSizeLarge}"
                                                    FontWeight="{StaticResource FontWeightBold}"
                                                    Foreground="{DynamicResource PrimaryCyanBrush}"
                                                    Text="3.8" />
                                                <TextBlock
                                                    HorizontalAlignment="Right"
                                                    FontSize="{DynamicResource FontSizeSmall}"
                                                    Foreground="{DynamicResource TextSecondaryBrush}"
                                                    Text="KDA" />
                                            </StackPanel>
                                        </Grid>

                                    </Border>

                                    <!--  英雄统计项 3  -->
                                    <Border
                                        Margin="0,0,0,10"
                                        Padding="15"
                                        Background="{DynamicResource BackgroundLightBrush}"
                                        BorderBrush="{DynamicResource BorderDarkBrush}"
                                        BorderThickness="1"
                                        Cursor="Hand">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="60" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <Border
                                                Grid.Column="0"
                                                Width="50"
                                                Height="50"
                                                Background="{DynamicResource PrimaryGoldBrush}"
                                                CornerRadius="2">
                                                <TextBlock
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    FontSize="10"
                                                    FontWeight="{StaticResource FontWeightBold}"
                                                    Foreground="{DynamicResource BackgroundDarkBrush}"
                                                    Text="阿卡丽" />
                                            </Border>

                                            <StackPanel
                                                Grid.Column="1"
                                                Margin="15,0,0,0"
                                                VerticalAlignment="Center">
                                                <TextBlock
                                                    Margin="0,0,0,5"
                                                    FontSize="{StaticResource FontSizeNormal}"
                                                    FontWeight="{StaticResource FontWeightBold}"
                                                    Foreground="{DynamicResource TextPrimaryBrush}"
                                                    Text="离群之刺 - 阿卡丽" />
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock
                                                        FontSize="{DynamicResource FontSizeSmall}"
                                                        Foreground="{DynamicResource TextSecondaryBrush}"
                                                        Text="54场 · " />
                                                    <TextBlock
                                                        FontSize="{DynamicResource FontSizeSmall}"
                                                        Foreground="{DynamicResource TextSecondaryBrush}"
                                                        Text="50% 胜率" />
                                                </StackPanel>
                                            </StackPanel>

                                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                                <TextBlock
                                                    HorizontalAlignment="Right"
                                                    FontSize="{StaticResource FontSizeLarge}"
                                                    FontWeight="{StaticResource FontWeightBold}"
                                                    Foreground="{DynamicResource TextPrimaryBrush}"
                                                    Text="3.5" />
                                                <TextBlock
                                                    HorizontalAlignment="Right"
                                                    FontSize="{DynamicResource FontSizeSmall}"
                                                    Foreground="{DynamicResource TextSecondaryBrush}"
                                                    Text="KDA" />
                                            </StackPanel>
                                        </Grid>

                                    </Border>
                                </StackPanel>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>

            <!--  底部操作栏  -->
            <Border
                Grid.Row="2"
                Padding="20,10"
                Background="{DynamicResource BackgroundMediumBrush}"
                BorderBrush="{DynamicResource BorderGoldBrush}"
                BorderThickness="0,2,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel
                        Grid.Column="0"
                        VerticalAlignment="Center"
                        Orientation="Horizontal">
                        <TextBlock
                            FontSize="{StaticResource FontSizeNormal}"
                            Foreground="{DynamicResource TextSecondaryBrush}"
                            Text="上次登录: " />
                        <TextBlock
                            Margin="5,0,0,0"
                            FontSize="{StaticResource FontSizeNormal}"
                            Foreground="{DynamicResource TextPrimaryBrush}"
                            Text="2024年1月30日 14:32" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button
                            Width="120"
                            Height="35"
                            Margin="0,0,10,0"
                            Content="编辑资料"
                            Style="{StaticResource LOLSecondaryButtonStyle}" />
                        <controls:GlowButton
                            Width="120"
                            Height="35"
                            Content="查看详情"
                            GlowColor="{DynamicResource PrimaryGold}" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
