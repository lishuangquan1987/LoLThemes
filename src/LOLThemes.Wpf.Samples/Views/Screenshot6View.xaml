<UserControl
    x:Class="LOLThemes.Wpf.Samples.Views.Screenshot6View"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:LOLThemes.Wpf.Controls;assembly=LOLThemes.Wpf"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="800"
    d:DesignWidth="1150"
    Background="{DynamicResource BackgroundDarkBrush}"
    mc:Ignorable="d">

    <Grid>
        <!--  主容器  -->
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  顶部标题栏  -->
            <Border
                Grid.Row="0"
                Padding="20,15"
                Background="{DynamicResource BackgroundMediumBrush}"
                BorderBrush="{DynamicResource BorderGoldBrush}"
                BorderThickness="0,0,0,2">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!--  左侧标题  -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock
                            Margin="0,0,15,0"
                            VerticalAlignment="Center"
                            FontSize="{DynamicResource FontSizeLarge}"
                            Foreground="{DynamicResource PrimaryGoldBrush}"
                            Text="🛒" />
                        <TextBlock
                            VerticalAlignment="Center"
                            FontSize="{StaticResource FontSizeLarge}"
                            FontWeight="{StaticResource FontWeightBold}"
                            Foreground="{DynamicResource TextPrimaryBrush}"
                            Text="商店" />
                    </StackPanel>

                    <!--  中间搜索框  -->
                    <TextBox
                        Grid.Column="1"
                        Width="350"
                        Height="35"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Style="{StaticResource LOLTextBoxStyle}"
                        Tag="搜索皮肤、英雄..." />

                    <!--  右侧货币信息  -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Border
                            x:Name="CurrencyCyanBorder"
                            Margin="0,0,10,0"
                            Padding="10,5"
                            Background="{DynamicResource PrimaryCyanBrush}"
                            Cursor="Hand">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontSize="{StaticResource FontSizeNormal}"
                                    Text="💎 " />
                                <TextBlock
                                    x:Name="CurrencyCyanText"
                                    VerticalAlignment="Center"
                                    FontSize="{StaticResource FontSizeNormal}"
                                    FontWeight="{StaticResource FontWeightBold}"
                                    Foreground="{DynamicResource BackgroundDarkBrush}"
                                    Text="2,450" />
                            </StackPanel>

                        </Border>
                        <Border
                            x:Name="CurrencyGoldBorder"
                            Padding="10,5"
                            Background="{DynamicResource PrimaryGoldBrush}"
                            Cursor="Hand">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontSize="{StaticResource FontSizeNormal}"
                                    Text="🪙 " />
                                <TextBlock
                                    x:Name="CurrencyGoldText"
                                    VerticalAlignment="Center"
                                    FontSize="{StaticResource FontSizeNormal}"
                                    FontWeight="{StaticResource FontWeightBold}"
                                    Foreground="{DynamicResource BackgroundDarkBrush}"
                                    Text="45,680" />
                            </StackPanel>

                        </Border>
                    </StackPanel>
                </Grid>
            </Border>

            <!--  主内容区域  -->
            <Grid Grid.Row="1" Margin="0,20,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!--  左侧分类菜单  -->
                <Border
                    Grid.Column="0"
                    Margin="0,0,10,0"
                    Padding="15"
                    Background="{DynamicResource BackgroundMediumBrush}"
                    BorderBrush="{DynamicResource BorderDarkBrush}"
                    BorderThickness="1">
                    <StackPanel>
                        <TextBlock
                            Margin="0,0,0,15"
                            FontSize="{StaticResource FontSizeMedium}"
                            FontWeight="{StaticResource FontWeightBold}"
                            Foreground="{DynamicResource PrimaryGoldBrush}"
                            Text="分类" />

                        <Button
                            Height="40"
                            Margin="0,0,0,8"
                            Content="精选"
                            Style="{StaticResource LOLPrimaryButtonStyle}" />
                        <Button
                            Height="40"
                            Margin="0,0,0,8"
                            Content="英雄"
                            Style="{StaticResource LOLSecondaryButtonStyle}" />
                        <Button
                            Height="40"
                            Margin="0,0,0,8"
                            Content="皮肤"
                            Style="{StaticResource LOLSecondaryButtonStyle}" />
                        <Button
                            Height="40"
                            Margin="0,0,0,8"
                            Content="表情"
                            Style="{StaticResource LOLSecondaryButtonStyle}" />
                        <Button
                            Height="40"
                            Margin="0,0,0,8"
                            Content="守卫"
                            Style="{StaticResource LOLSecondaryButtonStyle}" />
                        <Button
                            Height="40"
                            Margin="0,0,0,20"
                            Content="战利品"
                            Style="{StaticResource LOLSecondaryButtonStyle}" />

                        <Separator Margin="0,10,0,20" />

                        <TextBlock
                            Margin="0,0,0,15"
                            FontSize="{StaticResource FontSizeMedium}"
                            FontWeight="{StaticResource FontWeightBold}"
                            Foreground="{DynamicResource PrimaryGoldBrush}"
                            Text="筛选" />

                        <CheckBox
                            Margin="0,0,0,8"
                            Content="限时优惠"
                            IsChecked="True"
                            Style="{StaticResource LOLCheckBoxStyle}" />
                        <CheckBox
                            Margin="0,0,0,8"
                            Content="新品"
                            Style="{StaticResource LOLCheckBoxStyle}" />
                        <CheckBox
                            Margin="0,0,0,8"
                            Content="已拥有"
                            Style="{StaticResource LOLCheckBoxStyle}" />
                    </StackPanel>
                </Border>

                <!--  右侧商品展示区域  -->
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!--  精选横幅  -->
                    <Border
                        Grid.Row="0"
                        Height="200"
                        Margin="0,0,0,15"
                        Background="{DynamicResource BackgroundMediumBrush}"
                        BorderBrush="{DynamicResource BorderGoldBrush}"
                        BorderThickness="2">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="300" />
                            </Grid.ColumnDefinitions>

                            <!--  左侧：皮肤预览  -->
                            <Border
                                Grid.Column="0"
                                Margin="20"
                                Background="{DynamicResource BackgroundLightBrush}">
                                <TextBlock
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    FontSize="{DynamicResource FontSizeLarge}"
                                    FontWeight="{StaticResource FontWeightBold}"
                                    Foreground="{DynamicResource PrimaryGoldBrush}"
                                    Text="限定皮肤预览" />
                            </Border>

                            <!--  右侧：皮肤信息  -->
                            <StackPanel
                                Grid.Column="1"
                                Margin="0,20,20,20"
                                VerticalAlignment="Center">
                                <TextBlock
                                    Margin="0,0,0,10"
                                    FontSize="{StaticResource FontSizeLarge}"
                                    FontWeight="{StaticResource FontWeightBold}"
                                    Foreground="{DynamicResource PrimaryGoldBrush}"
                                    Text="至臻 K/DA 阿卡丽" />
                                <TextBlock
                                    Margin="0,0,0,15"
                                    FontSize="{StaticResource FontSizeNormal}"
                                    Foreground="{DynamicResource ErrorRedBrush}"
                                    Text="限时优惠" />
                                <StackPanel Margin="0,0,0,15" Orientation="Horizontal">
                                    <TextBlock
                                        Margin="0,0,10,0"
                                        FontSize="{DynamicResource FontSizeLarge}"
                                        FontWeight="{StaticResource FontWeightBold}"
                                        Foreground="{DynamicResource TextPrimaryBrush}"
                                        Text="1,820"
                                        TextDecorations="Strikethrough" />
                                    <TextBlock
                                        FontSize="{DynamicResource FontSizeLarge}"
                                        FontWeight="{StaticResource FontWeightBold}"
                                        Foreground="{DynamicResource SuccessGreenBrush}"
                                        Text="1,350" />
                                </StackPanel>
                                <controls:GlowButton
                                    Height="45"
                                    Content="立即购买"
                                    FontSize="{StaticResource FontSizeMedium}"
                                    FontWeight="{StaticResource FontWeightBold}"
                                    GlowColor="{DynamicResource PrimaryGold}" />
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!--  商品网格  -->
                    <Border
                        Grid.Row="1"
                        Padding="20"
                        Background="{DynamicResource BackgroundMediumBrush}"
                        BorderBrush="{DynamicResource BorderDarkBrush}"
                        BorderThickness="1">
                        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <TextBlock
                                    Margin="0,0,0,15"
                                    FontSize="{StaticResource FontSizeMedium}"
                                    FontWeight="{StaticResource FontWeightBold}"
                                    Foreground="{DynamicResource PrimaryGoldBrush}"
                                    Text="热门商品" />

                                <WrapPanel>
                                    <!--  商品卡片 1  -->
                                    <Border
                                        Width="180"
                                        Margin="0,0,15,15"
                                        Background="{DynamicResource BackgroundLightBrush}"
                                        BorderBrush="{DynamicResource BorderGoldBrush}"
                                        BorderThickness="1"
                                        RenderTransformOrigin="0.5,0.5">
                                        <Border.RenderTransform>
                                            <ScaleTransform ScaleX="1" ScaleY="1" />
                                        </Border.RenderTransform>

                                        <StackPanel>
                                            <Border Height="180" Background="{DynamicResource BackgroundDarkBrush}">
                                                <TextBlock
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    FontSize="{StaticResource FontSizeLarge}"
                                                    Foreground="{DynamicResource PrimaryGoldBrush}"
                                                    Text="皮肤" />
                                            </Border>
                                            <StackPanel Margin="10">
                                                <TextBlock
                                                    Margin="0,0,0,5"
                                                    FontSize="{StaticResource FontSizeNormal}"
                                                    FontWeight="{StaticResource FontWeightBold}"
                                                    Foreground="{DynamicResource TextPrimaryBrush}"
                                                    Text="源计划：亚索" />
                                                <TextBlock
                                                    Margin="0,0,0,8"
                                                    FontSize="{StaticResource FontSizeNormal}"
                                                    Foreground="{DynamicResource PrimaryCyanBrush}"
                                                    Text="1,350 RP" />
                                                <Button
                                                    Height="30"
                                                    Content="购买"
                                                    Style="{StaticResource LOLPrimaryButtonStyle}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!--  商品卡片 2  -->
                                    <Border
                                        Width="180"
                                        Margin="0,0,15,15"
                                        Background="{DynamicResource BackgroundLightBrush}"
                                        BorderBrush="{DynamicResource BorderDarkBrush}"
                                        BorderThickness="1"
                                        RenderTransformOrigin="0.5,0.5">
                                        <Border.RenderTransform>
                                            <ScaleTransform ScaleX="1" ScaleY="1" />
                                        </Border.RenderTransform>

                                        <StackPanel>
                                            <Border Height="180" Background="{DynamicResource BackgroundDarkBrush}">
                                                <TextBlock
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    FontSize="{StaticResource FontSizeLarge}"
                                                    Foreground="{DynamicResource PrimaryCyanBrush}"
                                                    Text="皮肤" />
                                            </Border>
                                            <StackPanel Margin="10">
                                                <TextBlock
                                                    Margin="0,0,0,5"
                                                    FontSize="{StaticResource FontSizeNormal}"
                                                    FontWeight="{StaticResource FontWeightBold}"
                                                    Foreground="{DynamicResource TextPrimaryBrush}"
                                                    Text="腥红之月：劫" />
                                                <TextBlock
                                                    Margin="0,0,0,8"
                                                    FontSize="{StaticResource FontSizeNormal}"
                                                    Foreground="{DynamicResource PrimaryCyanBrush}"
                                                    Text="1,350 RP" />
                                                <Button
                                                    Height="30"
                                                    Content="购买"
                                                    Style="{StaticResource LOLSecondaryButtonStyle}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!--  商品卡片 3  -->
                                    <Border
                                        Width="180"
                                        Margin="0,0,15,15"
                                        Background="{DynamicResource BackgroundLightBrush}"
                                        BorderBrush="{DynamicResource BorderDarkBrush}"
                                        BorderThickness="1"
                                        RenderTransformOrigin="0.5,0.5">
                                        <Border.RenderTransform>
                                            <ScaleTransform ScaleX="1" ScaleY="1" />
                                        </Border.RenderTransform>

                                        <StackPanel>
                                            <Border Height="180" Background="{DynamicResource BackgroundDarkBrush}">
                                                <TextBlock
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    FontSize="{StaticResource FontSizeLarge}"
                                                    Foreground="{DynamicResource PrimaryGoldBrush}"
                                                    Text="英雄" />
                                            </Border>
                                            <StackPanel Margin="10">
                                                <TextBlock
                                                    Margin="0,0,0,5"
                                                    FontSize="{StaticResource FontSizeNormal}"
                                                    FontWeight="{StaticResource FontWeightBold}"
                                                    Foreground="{DynamicResource TextPrimaryBrush}"
                                                    Text="阿卡丽" />
                                                <TextBlock
                                                    Margin="0,0,0,8"
                                                    FontSize="{StaticResource FontSizeNormal}"
                                                    Foreground="{DynamicResource PrimaryCyanBrush}"
                                                    Text="4,800 BE" />
                                                <Button
                                                    Height="30"
                                                    Content="购买"
                                                    Style="{StaticResource LOLSecondaryButtonStyle}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!--  商品卡片 4  -->
                                    <Border
                                        Width="180"
                                        Margin="0,0,15,15"
                                        Background="{DynamicResource BackgroundLightBrush}"
                                        BorderBrush="{DynamicResource BorderGoldBrush}"
                                        BorderThickness="1"
                                        RenderTransformOrigin="0.5,0.5">
                                        <Border.RenderTransform>
                                            <ScaleTransform ScaleX="1" ScaleY="1" />
                                        </Border.RenderTransform>

                                        <StackPanel>
                                            <Border Height="180" Background="{DynamicResource BackgroundDarkBrush}">
                                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                                    <TextBlock
                                                        Margin="0,0,0,5"
                                                        HorizontalAlignment="Center"
                                                        FontSize="{DynamicResource FontSizeSmall}"
                                                        Foreground="{DynamicResource ErrorRedBrush}"
                                                        Text="限时" />
                                                    <TextBlock
                                                        HorizontalAlignment="Center"
                                                        FontSize="{StaticResource FontSizeLarge}"
                                                        Foreground="{DynamicResource PrimaryGoldBrush}"
                                                        Text="皮肤" />
                                                </StackPanel>
                                            </Border>
                                            <StackPanel Margin="10">
                                                <TextBlock
                                                    Margin="0,0,0,5"
                                                    FontSize="{StaticResource FontSizeNormal}"
                                                    FontWeight="{StaticResource FontWeightBold}"
                                                    Foreground="{DynamicResource TextPrimaryBrush}"
                                                    Text="星之守护者：拉克丝" />
                                                <StackPanel Margin="0,0,0,8" Orientation="Horizontal">
                                                    <TextBlock
                                                        Margin="0,0,5,0"
                                                        FontSize="{DynamicResource FontSizeSmall}"
                                                        Foreground="{DynamicResource TextSecondaryBrush}"
                                                        Text="1,820"
                                                        TextDecorations="Strikethrough" />
                                                    <TextBlock
                                                        FontSize="{StaticResource FontSizeNormal}"
                                                        Foreground="{DynamicResource SuccessGreenBrush}"
                                                        Text="1,350 RP" />
                                                </StackPanel>
                                                <Button
                                                    Height="30"
                                                    Content="购买"
                                                    Style="{StaticResource LOLPrimaryButtonStyle}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!--  商品卡片 5  -->
                                    <Border
                                        Width="180"
                                        Margin="0,0,15,15"
                                        Background="{DynamicResource BackgroundLightBrush}"
                                        BorderBrush="{DynamicResource BorderDarkBrush}"
                                        BorderThickness="1"
                                        RenderTransformOrigin="0.5,0.5">
                                        <Border.RenderTransform>
                                            <ScaleTransform ScaleX="1" ScaleY="1" />
                                        </Border.RenderTransform>

                                        <StackPanel>
                                            <Border Height="180" Background="{DynamicResource BackgroundDarkBrush}">
                                                <TextBlock
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    FontSize="{StaticResource FontSizeLarge}"
                                                    Foreground="{DynamicResource PrimaryCyanBrush}"
                                                    Text="表情" />
                                            </Border>
                                            <StackPanel Margin="10">
                                                <TextBlock
                                                    Margin="0,0,0,5"
                                                    FontSize="{StaticResource FontSizeNormal}"
                                                    FontWeight="{StaticResource FontWeightBold}"
                                                    Foreground="{DynamicResource TextPrimaryBrush}"
                                                    Text="胜利手势" />
                                                <TextBlock
                                                    Margin="0,0,0,8"
                                                    FontSize="{StaticResource FontSizeNormal}"
                                                    Foreground="{DynamicResource PrimaryCyanBrush}"
                                                    Text="350 RP" />
                                                <Button
                                                    Height="30"
                                                    Content="购买"
                                                    Style="{StaticResource LOLSecondaryButtonStyle}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!--  商品卡片 6  -->
                                    <Border
                                        Width="180"
                                        Margin="0,0,15,15"
                                        Background="{DynamicResource BackgroundLightBrush}"
                                        BorderBrush="{DynamicResource BorderDarkBrush}"
                                        BorderThickness="1"
                                        RenderTransformOrigin="0.5,0.5">
                                        <Border.RenderTransform>
                                            <ScaleTransform ScaleX="1" ScaleY="1" />
                                        </Border.RenderTransform>

                                        <StackPanel>
                                            <Border Height="180" Background="{DynamicResource BackgroundDarkBrush}">
                                                <TextBlock
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    FontSize="{StaticResource FontSizeLarge}"
                                                    Foreground="{DynamicResource PrimaryGoldBrush}"
                                                    Text="守卫" />
                                            </Border>
                                            <StackPanel Margin="10">
                                                <TextBlock
                                                    Margin="0,0,0,5"
                                                    FontSize="{StaticResource FontSizeNormal}"
                                                    FontWeight="{StaticResource FontWeightBold}"
                                                    Foreground="{DynamicResource TextPrimaryBrush}"
                                                    Text="小小英雄" />
                                                <TextBlock
                                                    Margin="0,0,0,8"
                                                    FontSize="{StaticResource FontSizeNormal}"
                                                    Foreground="{DynamicResource PrimaryCyanBrush}"
                                                    Text="490 RP" />
                                                <Button
                                                    Height="30"
                                                    Content="购买"
                                                    Style="{StaticResource LOLSecondaryButtonStyle}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!--  商品卡片 7  -->
                                    <Border
                                        Width="180"
                                        Margin="0,0,15,15"
                                        Background="{DynamicResource BackgroundLightBrush}"
                                        BorderBrush="{DynamicResource BorderDarkBrush}"
                                        BorderThickness="1"
                                        RenderTransformOrigin="0.5,0.5">
                                        <Border.RenderTransform>
                                            <ScaleTransform ScaleX="1" ScaleY="1" />
                                        </Border.RenderTransform>

                                        <StackPanel>
                                            <Border Height="180" Background="{DynamicResource BackgroundDarkBrush}">
                                                <TextBlock
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    FontSize="{StaticResource FontSizeLarge}"
                                                    Foreground="{DynamicResource PrimaryCyanBrush}"
                                                    Text="英雄" />
                                            </Border>
                                            <StackPanel Margin="10">
                                                <TextBlock
                                                    Margin="0,0,0,5"
                                                    FontSize="{StaticResource FontSizeNormal}"
                                                    FontWeight="{StaticResource FontWeightBold}"
                                                    Foreground="{DynamicResource TextPrimaryBrush}"
                                                    Text="赛娜" />
                                                <TextBlock
                                                    Margin="0,0,0,8"
                                                    FontSize="{StaticResource FontSizeNormal}"
                                                    Foreground="{DynamicResource PrimaryCyanBrush}"
                                                    Text="6,300 BE" />
                                                <Button
                                                    Height="30"
                                                    Content="购买"
                                                    Style="{StaticResource LOLSecondaryButtonStyle}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!--  商品卡片 8  -->
                                    <Border
                                        Width="180"
                                        Margin="0,0,15,15"
                                        Background="{DynamicResource BackgroundLightBrush}"
                                        BorderBrush="{DynamicResource BorderDarkBrush}"
                                        BorderThickness="1"
                                        RenderTransformOrigin="0.5,0.5">
                                        <Border.RenderTransform>
                                            <ScaleTransform ScaleX="1" ScaleY="1" />
                                        </Border.RenderTransform>

                                        <StackPanel>
                                            <Border Height="180" Background="{DynamicResource BackgroundDarkBrush}">
                                                <TextBlock
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    FontSize="{StaticResource FontSizeLarge}"
                                                    Foreground="{DynamicResource PrimaryGoldBrush}"
                                                    Text="战利品" />
                                            </Border>
                                            <StackPanel Margin="10">
                                                <TextBlock
                                                    Margin="0,0,0,5"
                                                    FontSize="{StaticResource FontSizeNormal}"
                                                    FontWeight="{StaticResource FontWeightBold}"
                                                    Foreground="{DynamicResource TextPrimaryBrush}"
                                                    Text="海克斯宝箱" />
                                                <TextBlock
                                                    Margin="0,0,0,8"
                                                    FontSize="{StaticResource FontSizeNormal}"
                                                    Foreground="{DynamicResource PrimaryCyanBrush}"
                                                    Text="125 RP" />
                                                <Button
                                                    Height="30"
                                                    Content="购买"
                                                    Style="{StaticResource LOLSecondaryButtonStyle}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                </WrapPanel>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </Grid>

            <!--  底部状态栏  -->
            <Border
                Grid.Row="2"
                Padding="20,10"
                Background="{DynamicResource BackgroundMediumBrush}"
                BorderBrush="{DynamicResource BorderGoldBrush}"
                BorderThickness="0,2,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock
                            VerticalAlignment="Center"
                            FontSize="{StaticResource FontSizeNormal}"
                            Foreground="{DynamicResource TextSecondaryBrush}"
                            Text="购物车: " />
                        <TextBlock
                            Margin="5,0,20,0"
                            VerticalAlignment="Center"
                            FontSize="{StaticResource FontSizeNormal}"
                            FontWeight="{StaticResource FontWeightBold}"
                            Foreground="{DynamicResource PrimaryGoldBrush}"
                            Text="0 件商品" />

                        <TextBlock
                            VerticalAlignment="Center"
                            FontSize="{StaticResource FontSizeNormal}"
                            Foreground="{DynamicResource TextSecondaryBrush}"
                            Text="总计: " />
                        <TextBlock
                            VerticalAlignment="Center"
                            FontSize="{StaticResource FontSizeNormal}"
                            FontWeight="{StaticResource FontWeightBold}"
                            Foreground="{DynamicResource PrimaryCyanBrush}"
                            Text="{Binding PlayerData.RiotPoints, StringFormat='{}{0} RP'}" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button
                            Width="120"
                            Height="35"
                            Margin="0,0,10,0"
                            Content="查看购物车"
                            Style="{StaticResource LOLSecondaryButtonStyle}" />
                        <Button
                            Width="100"
                            Height="35"
                            Content="充值"
                            Style="{StaticResource LOLPrimaryButtonStyle}" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
