<UserControl x:Class="LOLThemes.Wpf.Samples.Views.SliderShowcaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:smtx="clr-namespace:ShowMeTheXAML;assembly=ShowMeTheXAML"
             Background="{DynamicResource BackgroundDarkBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="30">
        <StackPanel>
            <!-- 标题 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderGoldBrush}"
                   BorderThickness="0,0,0,2"
                   Padding="20,15"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Slider - 滑块" 
                          FontSize="{StaticResource FontSizeXXLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,8"/>
                    <TextBlock Text="滑块用于通过拖动来选择一个数值范围内的值。" 
                          FontSize="{StaticResource FontSizeNormal}"
                          Foreground="{DynamicResource TextSecondaryBrush}"/>
                </StackPanel>
            </Border>
            
            <!-- 基础用法 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="基础用法" 
                          FontSize="{StaticResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <smtx:XamlDisplay UniqueKey="SliderBasic" Margin="0,0,0,0">
                        <StackPanel>
                            <TextBlock Text="音量: 50" 
                                     Foreground="{DynamicResource TextPrimaryBrush}"
                                     Margin="0,0,0,10"/>
                            <Slider
                                  Minimum="0" Maximum="100" Value="50"
                                  Width="300" Height="30"
                                  HorizontalAlignment="Left"
                                  Margin="0,0,0,20"/>
                            
                            <TextBlock Text="透明度: 75" 
                                     Foreground="{DynamicResource TextPrimaryBrush}"
                                     Margin="0,0,0,10"/>
                            <Slider
                                  Minimum="0" Maximum="100" Value="75"
                                  Width="300" Height="30"
                                  HorizontalAlignment="Left"/>
                        </StackPanel>
                    </smtx:XamlDisplay>
                </StackPanel>
            </Border>
            
            <!-- 不同方向 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="不同方向" 
                          FontSize="{StaticResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <smtx:XamlDisplay UniqueKey="SliderOrientation" Margin="0,0,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="水平滑块" 
                                         Foreground="{DynamicResource TextPrimaryBrush}"
                                         Margin="0,0,0,10"/>
                                <Slider
                                      Minimum="0" Maximum="100" Value="50"
                                      Width="300" Height="30"
                                      HorizontalAlignment="Left"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Vertical">
                                <TextBlock Text="垂直滑块" 
                                         Foreground="{DynamicResource TextPrimaryBrush}"
                                         Margin="0,0,0,10"/>
                                <Slider
                                      Minimum="0" Maximum="100" Value="50"
                                      Orientation="Vertical"
                                      Width="30" Height="200"
                                      HorizontalAlignment="Left"/>
                            </StackPanel>
                        </Grid>
                    </smtx:XamlDisplay>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
