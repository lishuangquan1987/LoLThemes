<UserControl x:Class="LOLThemes.Wpf.Samples.Views.TextBoxShowcaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:smtx="clr-namespace:ShowMeTheXAML;assembly=ShowMeTheXAML"
             xmlns:helpers="clr-namespace:LOLThemes.Wpf.Helpers;assembly=LOLThemes.Wpf"
             Background="{DynamicResource BackgroundDarkBrush}">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="30">
        <StackPanel>
            <!-- 标题 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderGoldBrush}"
                   BorderThickness="0,0,0,2"
                   Padding="20,15"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="TextBox - 文本框" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,8"/>
                    <TextBlock Text="文本框用于接收用户输入的文本信息。" 
                          FontSize="{StaticResource FontSizeNormal}"
                          Foreground="{DynamicResource TextSecondaryBrush}"/>
                </StackPanel>
            </Border>
            
            <!-- 基础用法 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="基础用法" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <StackPanel>
                        <smtx:XamlDisplay Margin="0,0,0,15" UniqueKey="TextBoxPlaceholder">
                            <TextBox helpers:TextBoxHelper.Placeholder="请输入用户名..."
                                    Width="300" Height="40" 
                                    HorizontalAlignment="Left"/>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,15" UniqueKey="TextBoxFilled">
                            <TextBox Text="已填充的文本框"
                                    Width="300" Height="40" 
                                    HorizontalAlignment="Left"/>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="TextBoxMultiline">
                            <TextBox helpers:TextBoxHelper.Placeholder="请输入多行文本..."
                                    TextWrapping="Wrap"
                                    AcceptsReturn="True"
                                    VerticalScrollBarVisibility="Auto"
                                    Width="300" Height="100" 
                                    HorizontalAlignment="Left"/>
                        </smtx:XamlDisplay>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- 不同状态 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="不同状态" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <StackPanel>
                        <smtx:XamlDisplay Margin="0,0,0,15" UniqueKey="TextBoxNormalState">
                            <TextBox helpers:TextBoxHelper.Placeholder="正常状态"
                                    Width="300" Height="40" 
                                    HorizontalAlignment="Left"/>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="TextBoxDisabledState">
                            <TextBox helpers:TextBoxHelper.Placeholder="禁用状态"
                                    IsEnabled="False"
                                    Width="300" Height="40" 
                                    HorizontalAlignment="Left"/>
                        </smtx:XamlDisplay>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- 带清除按钮的文本框 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="带清除按钮的文本框" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <StackPanel>
                        <smtx:XamlDisplay Margin="0,0,0,15" UniqueKey="TextBoxWithClear">
                            <Grid>
                                <TextBox Text="可清除的文本内容" 
                                        Width="300" Height="40" 
                                        HorizontalAlignment="Left"/>
                                <Button Content="✕" 
                                        Width="30" Height="30" 
                                        HorizontalAlignment="Left"
                                        Margin="270,5,0,0"
                                        Background="Transparent"
                                        BorderBrush="Transparent"
                                        Foreground="{DynamicResource TextSecondaryBrush}"
                                        FontSize="16"
                                        Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Grid Background="{TemplateBinding Background}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </Grid>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="TextBoxWithClearPlaceholder">
                            <TextBox helpers:TextBoxHelper.Placeholder="输入后显示清除按钮" 
                                    Width="300" Height="40" 
                                    HorizontalAlignment="Left"/>
                        </smtx:XamlDisplay>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- 带图标的文本框 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="带图标的文本框" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <StackPanel>
                        <smtx:XamlDisplay Margin="0,0,0,15" UniqueKey="TextBoxSearchIcon">
                            <Grid>
                                <TextBox helpers:TextBoxHelper.Placeholder="搜索..." 
                                        Width="300" Height="40" 
                                        HorizontalAlignment="Left"/>
                                <TextBlock Text="🔍" 
                                          FontSize="18"
                                          Foreground="{DynamicResource TextSecondaryBrush}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Margin="10,5,0,0"/>
                            </Grid>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,15" UniqueKey="TextBoxUserIcon">
                            <Grid>
                                <TextBox helpers:TextBoxHelper.Placeholder="用户名" 
                                        Width="300" Height="40" 
                                        HorizontalAlignment="Left"/>
                                <TextBlock Text="👤" 
                                          FontSize="18"
                                          Foreground="{DynamicResource TextSecondaryBrush}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Margin="10,5,0,0"/>
                            </Grid>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="TextBoxEmailIcon">
                            <Grid>
                                <TextBox helpers:TextBoxHelper.Placeholder="邮箱" 
                                        Width="300" Height="40" 
                                        HorizontalAlignment="Left"/>
                                <TextBlock Text="📧" 
                                          FontSize="18"
                                          Foreground="{DynamicResource TextSecondaryBrush}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Margin="10,5,0,0"/>
                            </Grid>
                        </smtx:XamlDisplay>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- 密码框 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="密码框" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <StackPanel>
                        <smtx:XamlDisplay Margin="0,0,0,15" UniqueKey="TextBoxPasswordBasic">
                            <PasswordBox Width="300" Height="40" 
                                        HorizontalAlignment="Left"/>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="TextBoxPasswordWithIcon">
                            <Grid>
                                <PasswordBox Width="300" Height="40" 
                                            HorizontalAlignment="Left"/>
                                <TextBlock Text="🔒" 
                                          FontSize="18"
                                          Foreground="{DynamicResource TextSecondaryBrush}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Margin="10,5,0,0"/>
                            </Grid>
                        </smtx:XamlDisplay>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- 带验证的文本框 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="带验证的文本框" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <StackPanel>
                        <smtx:XamlDisplay Margin="0,0,0,15" UniqueKey="TextBoxValidationError">
                            <Grid>
                                <TextBox Text="invalid-email" 
                                        Width="300" Height="40" 
                                        HorizontalAlignment="Left"
                                        BorderBrush="{DynamicResource ErrorRedBrush}"
                                        BorderThickness="1.5"/>
                                <TextBlock Text="❌" 
                                          FontSize="18"
                                          Foreground="{DynamicResource ErrorRedBrush}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Margin="270,5,0,0"/>
                            </Grid>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="TextBoxValidationSuccess">
                            <Grid>
                                <TextBox Text="valid@email.com" 
                                        Width="300" Height="40" 
                                        HorizontalAlignment="Left"
                                        BorderBrush="{DynamicResource SuccessGreenBrush}"
                                        BorderThickness="1.5"/>
                                <TextBlock Text="✅" 
                                          FontSize="18"
                                          Foreground="{DynamicResource SuccessGreenBrush}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Margin="270,5,0,0"/>
                            </Grid>
                        </smtx:XamlDisplay>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- 文本框组 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="文本框组" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <StackPanel>
                        <TextBlock Text="用户信息" 
                                  FontSize="{DynamicResource FontSizeMedium}"
                                  FontWeight="{StaticResource FontWeightMedium}"
                                  Foreground="{DynamicResource TextPrimaryBrush}"
                                  Margin="0,0,0,15"/>
                        <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="TextBoxGroupUsername">
                            <Grid>
                                <TextBox helpers:TextBoxHelper.Placeholder="用户名" 
                                        Width="300" Height="40" 
                                        HorizontalAlignment="Left"/>
                                <TextBlock Text="👤" 
                                          FontSize="18"
                                          Foreground="{DynamicResource TextSecondaryBrush}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Margin="10,5,0,0"/>
                            </Grid>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,10" UniqueKey="TextBoxGroupEmail">
                            <Grid>
                                <TextBox helpers:TextBoxHelper.Placeholder="邮箱地址" 
                                        Width="300" Height="40" 
                                        HorizontalAlignment="Left"/>
                                <TextBlock Text="📧" 
                                          FontSize="18"
                                          Foreground="{DynamicResource TextSecondaryBrush}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Margin="10,5,0,0"/>
                            </Grid>
                        </smtx:XamlDisplay>

                        <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="TextBoxGroupPassword">
                            <Grid>
                                <PasswordBox helpers:TextBoxHelper.Placeholder="密码" 
                                            Width="300" Height="40" 
                                            HorizontalAlignment="Left"/>
                                <TextBlock Text="🔒" 
                                          FontSize="18"
                                          Foreground="{DynamicResource TextSecondaryBrush}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Margin="10,5,0,0"/>
                            </Grid>
                        </smtx:XamlDisplay>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- 带字符计数的文本框 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="带字符计数的文本框" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <smtx:XamlDisplay Margin="0,0,0,0" UniqueKey="TextBoxWithCharCount">
                        <Grid>
                            <TextBox Text="这是一段示例文本" 
                                    Width="300" Height="100" 
                                    TextWrapping="Wrap"
                                    AcceptsReturn="True"
                                    VerticalScrollBarVisibility="Auto"
                                    HorizontalAlignment="Left"/>
                            <TextBlock Text="8/100" 
                                      FontSize="{DynamicResource FontSizeSmall}"
                                      Foreground="{DynamicResource TextSecondaryBrush}"
                                      HorizontalAlignment="Right"
                                      VerticalAlignment="Bottom"
                                      Margin="0,0,5,5"/>
                        </Grid>
                    </smtx:XamlDisplay>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>

