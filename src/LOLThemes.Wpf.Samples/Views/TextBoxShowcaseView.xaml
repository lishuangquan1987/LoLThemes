<UserControl x:Class="LOLThemes.Wpf.Samples.Views.TextBoxShowcaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:smtx="clr-namespace:ShowMeTheXAML;assembly=ShowMeTheXAML"
             xmlns:helpers="clr-namespace:LOLThemes.Wpf.Helpers;assembly=LOLThemes.Wpf"
             Background="{DynamicResource BackgroundDarkBrush}">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="30">
        <StackPanel>
            <!-- 标题 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderGoldBrush}"
                   BorderThickness="0,0,0,2"
                   Padding="20,15"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="TextBox - 文本框" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,8"/>
                    <TextBlock Text="文本框用于接收用户输入的文本信息。" 
                          FontSize="{StaticResource FontSizeNormal}"
                          Foreground="{DynamicResource TextSecondaryBrush}"/>
                </StackPanel>
            </Border>
            
            <!-- 基础用法 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="基础用法" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <smtx:XamlDisplay UniqueKey="TextBoxBasic" Margin="0,0,0,0">
                <StackPanel>
                    <TextBox helpers:TextBoxHelper.Placeholder="请输入用户名..."
                            Width="300" Height="40" 
                            HorizontalAlignment="Left"
                            Margin="0,0,0,15"/>
                    <TextBox Text="已填充的文本框"
                            Width="300" Height="40" 
                            HorizontalAlignment="Left"
                            Margin="0,0,0,15"/>
                    <TextBox helpers:TextBoxHelper.Placeholder="请输入多行文本..."
                            TextWrapping="Wrap"
                            AcceptsReturn="True"
                            VerticalScrollBarVisibility="Auto"
                            Width="300" Height="100" 
                            HorizontalAlignment="Left"/>
                </StackPanel>
                    </smtx:XamlDisplay>
                </StackPanel>
            </Border>
            
            <!-- 不同状态 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="不同状态" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <smtx:XamlDisplay UniqueKey="TextBoxStates" Margin="0,0,0,0">
                <StackPanel>
                    <TextBox helpers:TextBoxHelper.Placeholder="正常状态"
                            Width="300" Height="40" 
                            HorizontalAlignment="Left"
                            Margin="0,0,0,15"/>
                    <TextBox helpers:TextBoxHelper.Placeholder="禁用状态"
                            IsEnabled="False"
                            Width="300" Height="40" 
                            HorizontalAlignment="Left"/>
                </StackPanel>
                    </smtx:XamlDisplay>
                </StackPanel>
            </Border>
            
            <!-- 带清除按钮的文本框 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="带清除按钮的文本框" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <smtx:XamlDisplay UniqueKey="TextBoxWithClearButton" Margin="0,0,0,0">
                        <StackPanel>
                            <Grid>
                                <TextBox Text="可清除的文本内容" 
                                        Width="300" Height="40" 
                                        HorizontalAlignment="Left"
                                        Margin="0,0,0,15"/>
                                <Button Content="✕" 
                                        Width="30" Height="30" 
                                        HorizontalAlignment="Left"
                                        Margin="270,5,0,15"
                                        Background="Transparent"
                                        BorderBrush="Transparent"
                                        Foreground="{DynamicResource TextSecondaryBrush}"
                                        FontSize="16"
                                        Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Grid Background="{TemplateBinding Background}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </Grid>
                            <TextBox helpers:TextBoxHelper.Placeholder="输入后显示清除按钮" 
                                    Width="300" Height="40" 
                                    HorizontalAlignment="Left"/>
                        </StackPanel>
                    </smtx:XamlDisplay>
                </StackPanel>
            </Border>
            
            <!-- 带图标的文本框 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="带图标的文本框" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <smtx:XamlDisplay UniqueKey="TextBoxWithIcon" Margin="0,0,0,0">
                        <StackPanel>
                            <Grid>
                                <TextBox helpers:TextBoxHelper.Placeholder="搜索..." 
                                        Width="300" Height="40" 
                                        HorizontalAlignment="Left"
                                        Margin="0,0,0,15"/>
                                <TextBlock Text="🔍" 
                                          FontSize="18"
                                          Foreground="{DynamicResource TextSecondaryBrush}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Margin="10,5,0,15"/>
                            </Grid>
                            <Grid>
                                <TextBox helpers:TextBoxHelper.Placeholder="用户名" 
                                        Width="300" Height="40" 
                                        HorizontalAlignment="Left"
                                        Margin="0,0,0,15"/>
                                <TextBlock Text="👤" 
                                          FontSize="18"
                                          Foreground="{DynamicResource TextSecondaryBrush}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Margin="10,5,0,15"/>
                            </Grid>
                            <Grid>
                                <TextBox helpers:TextBoxHelper.Placeholder="邮箱" 
                                        Width="300" Height="40" 
                                        HorizontalAlignment="Left"/>
                                <TextBlock Text="📧" 
                                          FontSize="18"
                                          Foreground="{DynamicResource TextSecondaryBrush}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Margin="10,5,0,0"/>
                            </Grid>
                        </StackPanel>
                    </smtx:XamlDisplay>
                </StackPanel>
            </Border>
            
            <!-- 密码框 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="密码框" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <smtx:XamlDisplay UniqueKey="PasswordBox" Margin="0,0,0,0">
                        <StackPanel>
                            <PasswordBox Width="300" Height="40" 
                                        HorizontalAlignment="Left"
                                        Margin="0,0,0,15"/>
                            <Grid>
                                <PasswordBox Width="300" Height="40" 
                                            HorizontalAlignment="Left"/>
                                <TextBlock Text="🔒" 
                                          FontSize="18"
                                          Foreground="{DynamicResource TextSecondaryBrush}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Margin="10,5,0,0"/>
                            </Grid>
                        </StackPanel>
                    </smtx:XamlDisplay>
                </StackPanel>
            </Border>
            
            <!-- 带验证的文本框 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="带验证的文本框" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <smtx:XamlDisplay UniqueKey="TextBoxWithValidation" Margin="0,0,0,0">
                        <StackPanel>
                            <Grid>
                                <TextBox Text="invalid-email" 
                                        Width="300" Height="40" 
                                        HorizontalAlignment="Left"
                                        Margin="0,0,0,15"
                                        BorderBrush="{DynamicResource ErrorRedBrush}"
                                        BorderThickness="1.5"/>
                                <TextBlock Text="❌" 
                                          FontSize="18"
                                          Foreground="{DynamicResource ErrorRedBrush}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Margin="270,5,0,15"/>
                            </Grid>
                            <Grid>
                                <TextBox Text="valid@email.com" 
                                        Width="300" Height="40" 
                                        HorizontalAlignment="Left"
                                        Margin="0,0,0,15"
                                        BorderBrush="{DynamicResource SuccessGreenBrush}"
                                        BorderThickness="1.5"/>
                                <TextBlock Text="✅" 
                                          FontSize="18"
                                          Foreground="{DynamicResource SuccessGreenBrush}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Margin="270,5,0,15"/>
                            </Grid>
                        </StackPanel>
                    </smtx:XamlDisplay>
                </StackPanel>
            </Border>
            
            <!-- 文本框组 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="文本框组" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <smtx:XamlDisplay UniqueKey="TextBoxGroup" Margin="0,0,0,0">
                        <StackPanel>
                            <TextBlock Text="用户信息" 
                                      FontSize="{DynamicResource FontSizeMedium}"
                                      FontWeight="{StaticResource FontWeightMedium}"
                                      Foreground="{DynamicResource TextPrimaryBrush}"
                                      Margin="0,0,0,15"/>
                            <Grid>
                                <TextBox helpers:TextBoxHelper.Placeholder="用户名" 
                                        Width="300" Height="40" 
                                        HorizontalAlignment="Left"
                                        Margin="0,0,0,10"/>
                                <TextBlock Text="👤" 
                                          FontSize="18"
                                          Foreground="{DynamicResource TextSecondaryBrush}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Margin="10,5,0,10"/>
                            </Grid>
                            <Grid>
                                <TextBox helpers:TextBoxHelper.Placeholder="邮箱地址" 
                                        Width="300" Height="40" 
                                        HorizontalAlignment="Left"
                                        Margin="0,0,0,10"/>
                                <TextBlock Text="📧" 
                                          FontSize="18"
                                          Foreground="{DynamicResource TextSecondaryBrush}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Margin="10,5,0,10"/>
                            </Grid>
                            <Grid>
                                <PasswordBox helpers:TextBoxHelper.Placeholder="密码" 
                                            Width="300" Height="40" 
                                            HorizontalAlignment="Left"/>
                                <TextBlock Text="🔒" 
                                          FontSize="18"
                                          Foreground="{DynamicResource TextSecondaryBrush}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Margin="10,5,0,0"/>
                            </Grid>
                        </StackPanel>
                    </smtx:XamlDisplay>
                </StackPanel>
            </Border>
            
            <!-- 带字符计数的文本框 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="带字符计数的文本框" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <smtx:XamlDisplay UniqueKey="TextBoxWithCharCount" Margin="0,0,0,0">
                        <StackPanel>
                            <Grid>
                                <TextBox Text="这是一段示例文本" 
                                        Width="300" Height="100" 
                                        TextWrapping="Wrap"
                                        AcceptsReturn="True"
                                        VerticalScrollBarVisibility="Auto"
                                        HorizontalAlignment="Left"/>
                                <TextBlock Text="8/100" 
                                          FontSize="{DynamicResource FontSizeSmall}"
                                          Foreground="{DynamicResource TextSecondaryBrush}"
                                          HorizontalAlignment="Right"
                                          VerticalAlignment="Bottom"
                                          Margin="0,0,5,5"/>
                            </Grid>
                        </StackPanel>
                    </smtx:XamlDisplay>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>

