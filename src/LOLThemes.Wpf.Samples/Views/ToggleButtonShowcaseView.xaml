<UserControl x:Class="LOLThemes.Wpf.Samples.Views.ToggleButtonShowcaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:smtx="clr-namespace:ShowMeTheXAML;assembly=ShowMeTheXAML"
             Background="{DynamicResource BackgroundDarkBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="30">
        <StackPanel>
            <!-- 标题 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderGoldBrush}"
                   BorderThickness="0,0,0,2"
                   Padding="20,15"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="ToggleButton - 切换按钮" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,8"/>
                    <TextBlock Text="切换按钮用于在两种状态之间切换，类似于开关。" 
                          FontSize="{StaticResource FontSizeNormal}"
                          Foreground="{DynamicResource TextSecondaryBrush}"/>
                </StackPanel>
            </Border>
            
            <!-- 基础用法 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="基础用法" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <smtx:XamlDisplay UniqueKey="ToggleButtonOption1" Margin="0,0,0,10">
                        <ToggleButton Content="切换按钮" 
                                    Width="150" Height="40"/>
                    </smtx:XamlDisplay>
                    
                    <smtx:XamlDisplay UniqueKey="ToggleButtonChecked" Margin="0,0,0,0">
                        <ToggleButton Content="已选中" 
                                    IsChecked="True"
                                    Width="150" Height="40"/>
                    </smtx:XamlDisplay>
                </StackPanel>
            </Border>
            
            <!-- 不同状态 -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                   BorderBrush="{DynamicResource BorderDarkBrush}"
                   BorderThickness="0.5"
                   Padding="20"
                   Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="不同状态" 
                          FontSize="{DynamicResource FontSizeLarge}"
                          FontWeight="{StaticResource FontWeightBold}"
                          Foreground="{DynamicResource PrimaryGoldBrush}"
                          Margin="0,0,0,15"/>
                    
                    <smtx:XamlDisplay UniqueKey="ToggleButtonUnchecked" Margin="0,0,0,10">
                        <ToggleButton Content="未选中" 
                                    Width="150" Height="40"/>
                    </smtx:XamlDisplay>
                    
                    <smtx:XamlDisplay UniqueKey="ToggleButtonCheckedState" Margin="0,0,0,10">
                        <ToggleButton Content="已选中" 
                                    IsChecked="True"
                                    Width="150" Height="40"/>
                    </smtx:XamlDisplay>
                    
                    <smtx:XamlDisplay UniqueKey="ToggleButtonDisabled" Margin="0,0,0,0">
                        <ToggleButton Content="禁用状态" 
                                    IsEnabled="False"
                                    Width="150" Height="40"/>
                    </smtx:XamlDisplay>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
