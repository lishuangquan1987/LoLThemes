<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- 合并基础资源，确保可以访�和Colors �和Fonts -->
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/LOLThemes.Wpf;component/Themes/Colors.xaml"/>
        <ResourceDictionary Source="/LOLThemes.Wpf;component/Themes/Fonts.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- Menu Separator Style -->
    <Style x:Key="MenuSeparatorStyle" TargetType="{x:Type Separator}">
        <Setter Property="Background" Value="{DynamicResource BorderDarkBrush}"/>
        <Setter Property="Height" Value="1"/>
        <Setter Property="Margin" Value="{DynamicResource ItemMargin}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Separator}">
                    <Border Background="{TemplateBinding Background}"
                            Height="{TemplateBinding Height}"
                            Margin="{TemplateBinding Margin}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- MenuItem Style -->
    <Style x:Key="LOLMenuItemStyle" TargetType="{x:Type MenuItem}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource TextBrightBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFont}"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeNormal}"/>
        <Setter Property="Padding" Value="{DynamicResource MenuItemPadding}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type MenuItem}">
                    <Border x:Name="Border"
                            BorderBrush="Transparent"
                            BorderThickness="0">
                        <Border.Background>
                            <DynamicResource ResourceKey="DarkGradientBrush"/>
                        </Border.Background>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Icon"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Shortcut"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Icon -->
                            <ContentPresenter Grid.Column="0"
                                            x:Name="Icon"
                                            ContentSource="Icon"
                                            Margin="8,0,8,0"
                                            VerticalAlignment="Center"/>

                            <!-- Check Mark -->
                            <Path Grid.Column="0"
                                  x:Name="CheckMark"
                                  Data="M0,5 L3,8 L8,0"
                                  Stroke="{DynamicResource PrimaryGoldBrush}"
                                  StrokeThickness="2"
                                  Visibility="Collapsed"
                                  Margin="8,0,8,0"
                                  VerticalAlignment="Center"/>

                            <!-- Header -->
                            <ContentPresenter Grid.Column="1"
                                            ContentSource="Header"
                                            RecognizesAccessKey="True"
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"
                                            TextElement.Foreground="{TemplateBinding Foreground}"/>

                            <!-- Input Gesture Text (Shortcut) -->
                            <TextBlock Grid.Column="2"
                                     x:Name="InputGestureText"
                                     Text="{TemplateBinding InputGestureText}"
                                     Foreground="{DynamicResource TextSecondaryBrush}"
                                     Margin="16,0,8,0"
                                     VerticalAlignment="Center"/>

                            <!-- Submenu Arrow -->
                            <Path Grid.Column="3"
                                  x:Name="SubMenuArrow"
                                  Data="M0,0 L4,4 L0,8 Z"
                                  Fill="{DynamicResource TextBrightBrush}"
                                  Visibility="Collapsed"
                                  Margin="0,0,8,0"
                                  VerticalAlignment="Center"/>

                            <!-- Popup for Submenu -->
                            <Popup Grid.Column="0"
                                   x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   Placement="Right"
                                   IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Border Background="{DynamicResource BackgroundMediumBrush}"
                                        BorderBrush="{DynamicResource BorderGoldBrush}"
                                        BorderThickness="1"
                                        CornerRadius="2"
                                        Padding="2">
                                    <Border.Effect>
                                        <DropShadowEffect Color="{DynamicResource PrimaryGold}" 
                                                        BlurRadius="10" 
                                                        ShadowDepth="0" 
                                                        Opacity="0.3"/>
                                    </Border.Effect>
                                    <ScrollViewer Style="{DynamicResource {ComponentResourceKey TypeInTargetAssembly={x:Type FrameworkElement}, ResourceId=MenuScrollViewer}}"
                                                CanContentScroll="True">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <!-- Top Level Item -->
                        <Trigger Property="Role" Value="TopLevelHeader">
                            <Setter Property="Padding" Value="{DynamicResource MenuItemPadding}"/>
                            <Setter TargetName="PART_Popup" Property="Placement" Value="Bottom"/>
                        </Trigger>

                        <Trigger Property="Role" Value="TopLevelItem">
                            <Setter Property="Padding" Value="{DynamicResource MenuItemPadding}"/>
                        </Trigger>

                        <!-- Submenu Item -->
                        <Trigger Property="Role" Value="SubmenuHeader">
                            <Setter TargetName="SubMenuArrow" Property="Visibility" Value="Visible"/>
                        </Trigger>

                        <!-- Checkable -->
                        <Trigger Property="IsCheckable" Value="True">
                            <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                        </Trigger>

                        <Trigger Property="IsChecked" Value="False">
                            <Setter TargetName="CheckMark" Property="Visibility" Value="Collapsed"/>
                        </Trigger>

                        <!-- Has Icon -->
                        <Trigger Property="Icon" Value="{x:Null}">
                            <Setter TargetName="Icon" Property="Visibility" Value="Collapsed"/>
                        </Trigger>

                        <!-- Mouse Over -->
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource GoldGradientBrush}"/>
                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource BorderGoldBrush}"/>
                            <Setter TargetName="Border" Property="BorderThickness" Value="{DynamicResource BorderThicknessMedium}"/>
                            <Setter Property="Foreground" Value="{DynamicResource BackgroundDarkBrush}"/>
                            <Setter TargetName="SubMenuArrow" Property="Fill" Value="{DynamicResource BackgroundDarkBrush}"/>
                            <Setter TargetName="InputGestureText" Property="Foreground" Value="{DynamicResource BackgroundDarkBrush}"/>
                        </Trigger>

                        <!-- Disabled -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource TextDisabledBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Menu Style -->
    <Style x:Key="LOLMenuStyle" TargetType="{x:Type Menu}">
        <Setter Property="Background" Value="{DynamicResource BackgroundMediumBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderDarkBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,{DynamicResource BorderThicknessMedium}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFont}"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeNormal}"/>
        <!-- 不设置 ItemContainerStyle，让 MenuItem 使用隐式样式，Separator 使用默认样式 -->
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Menu}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="4,2">
                        <ItemsPresenter/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 默认 Menu 样式 -->
    <Style TargetType="{x:Type Menu}" BasedOn="{StaticResource LOLMenuStyle}"/>
    <!-- 默认 MenuItem 样式 -->
    <Style TargetType="{x:Type MenuItem}" BasedOn="{StaticResource LOLMenuItemStyle}"/>

</ResourceDictionary>
