<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- LOL ProgressBar 样式 -->
    <Style x:Key="LOLProgressBarStyle" TargetType="ProgressBar">
        <Setter Property="Height" Value="20"/>
        <Setter Property="Background" Value="{StaticResource BackgroundMediumBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryGoldBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderDarkBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Grid>
                        <!-- 背景边框 -->
                        <Border Background="{TemplateBinding Background}"
                               BorderBrush="{TemplateBinding BorderBrush}"
                               BorderThickness="{TemplateBinding BorderThickness}"
                               CornerRadius="2"
                               SnapsToDevicePixels="True"/>
                        
                        <!-- 进度条容器 -->
                        <Border x:Name="PART_Track"
                               Margin="2"
                               CornerRadius="1">
                            <Grid>
                                <!-- 进度指示器 -->
                                <Border x:Name="PART_Indicator"
                                       HorizontalAlignment="Left"
                                       CornerRadius="1">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="{StaticResource PrimaryGold}" Offset="0"/>
                                            <GradientStop Color="{StaticResource HoverGold}" Offset="0.5"/>
                                            <GradientStop Color="{StaticResource PrimaryGold}" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>
                                
                                <!-- 不确定模式的动画 -->
                                <Border x:Name="PART_GlowRect"
                                       HorizontalAlignment="Left"
                                       Width="100"
                                       Margin="-100,0,0,0"
                                       CornerRadius="1"
                                       Visibility="Collapsed">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="Transparent" Offset="0"/>
                                            <GradientStop Color="{StaticResource PrimaryGold}" Offset="0.5"/>
                                            <GradientStop Color="Transparent" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <Border.RenderTransform>
                                        <TranslateTransform x:Name="GlowTransform" X="0"/>
                                    </Border.RenderTransform>
                                </Border>
                            </Grid>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!-- 不确定模式 -->
                        <Trigger Property="IsIndeterminate" Value="True">
                            <Setter TargetName="PART_Indicator" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="PART_GlowRect" Property="Visibility" Value="Visible"/>
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetName="GlowTransform"
                                                       Storyboard.TargetProperty="X"
                                                       From="0"
                                                       To="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                                       Duration="0:0:1.5"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 垂直 ProgressBar 样式 -->
    <Style x:Key="LOLVerticalProgressBarStyle" TargetType="ProgressBar">
        <Setter Property="Width" Value="20"/>
        <Setter Property="Background" Value="{StaticResource BackgroundMediumBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryGoldBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderDarkBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Orientation" Value="Vertical"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Grid>
                        <!-- 背景边框 -->
                        <Border Background="{TemplateBinding Background}"
                               BorderBrush="{TemplateBinding BorderBrush}"
                               BorderThickness="{TemplateBinding BorderThickness}"
                               CornerRadius="2"
                               SnapsToDevicePixels="True"/>
                        
                        <!-- 进度条容器 -->
                        <Border x:Name="PART_Track"
                               Margin="2"
                               CornerRadius="1">
                            <Grid>
                                <!-- 进度指示器 -->
                                <Border x:Name="PART_Indicator"
                                       VerticalAlignment="Bottom"
                                       CornerRadius="1">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,1" EndPoint="0,0">
                                            <GradientStop Color="{StaticResource PrimaryGold}" Offset="0"/>
                                            <GradientStop Color="{StaticResource HoverGold}" Offset="0.5"/>
                                            <GradientStop Color="{StaticResource PrimaryGold}" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>
                            </Grid>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
