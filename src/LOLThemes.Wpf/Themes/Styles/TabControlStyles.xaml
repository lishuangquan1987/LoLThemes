<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!--
        ===================================================================
        TabControlStyles.xaml - TabControl 控件样式定义
        ===================================================================
        
        文件说明：
        - 定义 TabControl 和 TabItem 控件的视觉样式
        - 根据 101.qq.com 网站配色重新设计
        - 选中项显示底部青色下划线
        
        包含的样式：
        - LOLTabItemStyle: TabItem 样式
        - LOLTabControlStyle: TabControl 容器样式
        - 默认样式: 自动应用到所有 TabControl 和 TabItem 控件
        
        设计特点：
        - 选中项显示底部 3px 青色下划线（AccentCyan）
        - 选中项文本颜色为青色（AccentCyan）
        - 未选中项文本颜色为次要文本色（TextSecondary）
        - 移除所有动画效果（下划线为静态显示）
        - 移除所有阴影效果
        
        使用方式：
        - 默认样式会自动应用到所有 TabControl 和 TabItem 控件
        - 每个 TabItem 包含自己的 SelectedIndicator，静态显示
    -->
    
    <!-- 合并基础资源，确保可以访问 Fonts -->
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/LOLThemes.Wpf;component/Themes/Fonts.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- TabItem 样式 -->
    <Style x:Key="LOLTabItemStyle" TargetType="TabItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderDarkBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFont}"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeNormal}"/>
        <Setter Property="FontWeight" Value="{StaticResource FontWeightMedium}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Grid x:Name="Root">
                        <Border x:Name="Border"
                               BorderBrush="{TemplateBinding BorderBrush}"
                               BorderThickness="{TemplateBinding BorderThickness}"
                               Padding="{TemplateBinding Padding}"
                               Margin="0,0,2,0"
                               SnapsToDevicePixels="True"
                               UseLayoutRounding="True"
                               Background="Transparent">
                            <ContentPresenter x:Name="ContentSite"
                                            ContentSource="Header"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            RecognizesAccessKey="True"
                                            TextOptions.TextFormattingMode="Display"
                                            TextOptions.TextRenderingMode="ClearType"
                                            RenderOptions.ClearTypeHint="Enabled"
                                            SnapsToDevicePixels="True"/>
                        </Border>
                        
                        <!-- 选中指示器 - 青色下划线 -->
                        <Border x:Name="SelectedIndicator"
                               Height="3"
                               VerticalAlignment="Bottom"
                               Background="{DynamicResource SelectedAccentBrush}"
                               Opacity="0"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!-- 鼠标悬停 - 无动画，即时切换 -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" 
                                   Value="Transparent"/>
                            <Setter Property="Foreground" 
                                   Value="{DynamicResource TextPrimaryBrush}"/>
                        </Trigger>
                        
                        <!-- 选中状态 - 青色下划线 -->
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Border" Property="Background" 
                                   Value="Transparent"/>
                            <Setter Property="Foreground" 
                                   Value="{DynamicResource SelectedAccentBrush}"/>
                            <Setter Property="Panel.ZIndex" Value="100"/>
                            <Setter TargetName="SelectedIndicator" Property="Opacity" Value="1"/>
                        </Trigger>
                        
                        <!-- 禁用状态 -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" 
                                   Value="{DynamicResource TextDisabledBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- LOL TabControl 样式 -->
    <Style x:Key="LOLTabControlStyle" TargetType="TabControl">
        <Setter Property="Background" Value="{DynamicResource BackgroundDarkBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderDefaultBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <!-- 选项卡头部区域 -->
                        <Border Grid.Row="0"
                               Background="{DynamicResource BackgroundDarkBrush}"
                               BorderBrush="{TemplateBinding BorderBrush}"
                               BorderThickness="0,0,0,1">
                            <TabPanel x:Name="HeaderPanel"
                                     IsItemsHost="True"
                                     Margin="2,0,2,0"/>
                        </Border>
                        
                        <!-- 内容区域 -->
                        <Border x:Name="ContentPanel"
                               Grid.Row="1"
                               Background="{TemplateBinding Background}"
                               BorderBrush="{TemplateBinding BorderBrush}"
                               BorderThickness="{TemplateBinding BorderThickness}"
                               Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="PART_SelectedContentHost"
                                            ContentSource="SelectedContent"
                                            Margin="6"
                                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemContainerStyle" Value="{StaticResource LOLTabItemStyle}"/>
    </Style>

    <!-- 默认 TabControl 样式 -->
    <Style TargetType="{x:Type TabControl}" BasedOn="{StaticResource LOLTabControlStyle}"/>
    <!-- 默认 TabItem 样式 -->
    <Style TargetType="{x:Type TabItem}" BasedOn="{StaticResource LOLTabItemStyle}"/>

</ResourceDictionary>
